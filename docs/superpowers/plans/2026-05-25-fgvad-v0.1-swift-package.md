# fgvad v0.1 Swift Package(本地能用)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 fgvad 从"两份 demo 各拷一份 Swift wrapper"形态收成 `Sources/Fgvad/` 单点 +
打 `FgvadCore.xcframework` / `TenVad.xcframework`,iOS/macOS demo 通过 Swift Package 消费 `import Fgvad`,
跑通端到端。**完成态:本地 SPM 路径接入可用,GitHub Release 留给 Plan 2**。

**Architecture:** Swift wrapper 写在 `Sources/Fgvad/`,通过 `module.modulemap` 看 fgvad.h(C ABI),
两份 XCFramework(fgvad staticlib 三 slice + ten\_vad framework 三 slice)放在 `dist/`,
Package.swift 用 path-based binaryTarget 引用,demo 把自己内部的 FgVadAnalyzer.swift 拷贝替换成 `import Fgvad`。

**Tech Stack:** Swift 5.9 + SPM(binary target)+ xcodebuild -create-xcframework + cargo (3 target: aarch64-apple-ios / aarch64-apple-ios-sim / 已有 universal-apple-darwin)

---

## File Structure

**新建:**

| 路径 | 责任 |
|---|---|
| `include/module.modulemap` | 暴露 fgvad.h 成 `CFgvad` C module(给 Swift target 用) |
| `Sources/Fgvad/FgVadAnalyzer.swift` | Swift wrapper(public API),从 demo 抽过来,改成 `import CFgvad` |
| `Package.swift` | SPM manifest,path-based binaryTarget(dist/ 下两个 .xcframework) |
| `scripts/build-xcframework.sh` | 编 fgvad 三 slice + 合 FgvadCore.xcframework + 合 TenVad.xcframework |
| `dist/.gitkeep` | 占位,xcframework 产物 ignore |

**修改:**

| 路径 | 改什么 |
|---|---|
| `examples/ios/FgVadDemo/project.yml` | 删 fgvad.h 的 HEADER_SEARCH_PATHS / LIBRARY_SEARCH_PATHS / FRAMEWORK_SEARCH_PATHS / OTHER_LDFLAGS / preBuildScripts;加 packages.Fgvad: path,dependencies 加 Fgvad |
| `examples/ios/FgVadDemo/FgVadDemo/FgVadDemo-Bridging-Header.h` | 删 `#import "fgvad.h"`(SPM 暴露 Swift module 后不需要),保留 OC 录音器 import |
| `examples/ios/FgVadDemo/FgVadDemo/FgVadAnalyzer.swift` | 删除(收到 Sources/Fgvad/ 单点) |
| `examples/macos/project.yml` | 删 ten\_vad framework dependency / HEADER\_SEARCH\_PATHS / LIBRARY\_SEARCH\_PATHS / OTHER\_LDFLAGS / preBuildScripts;加 packages.Fgvad: path,dependencies 加 Fgvad |
| `examples/macos/FgVadDemo-Bridging-Header.h` | 删 `#import "fgvad.h"`(macOS demo 没用 OC,改成空头文件或干脆从 project.yml 移除) |
| `examples/macos/FgVadDemo/FgVadAnalyzer.swift` | 删除(收到 Sources/Fgvad/ 单点) |
| `.gitignore` | 加 `dist/*.xcframework/` 和 `dist/*.zip`(产物不入库,只入 .gitkeep) |

---

## Task 1: include/module.modulemap

**目标:** 给 fgvad.h 写一个 module map,让它能被 Swift target 当作 `import CFgvad` 用。
XCFramework 包 headers 时会自动复制 modulemap。

**Files:**
- Create: `include/module.modulemap`

- [ ] **Step 1: 写 modulemap**

`include/module.modulemap`:
```
module CFgvad {
    header "fgvad.h"
    export *
}
```

- [ ] **Step 2: 验证 modulemap 语法(可选 sanity check)**

```bash
clang -fmodules -fsyntax-only -x c \
  -I/Users/fengurwang/Desktop/OldJob/fgvad/include \
  -include /Users/fengurwang/Desktop/OldJob/fgvad/include/fgvad.h \
  -o /tmp/null - <<< 'int main(void){ return 0; }'
echo "exit=$?"
```

预期:exit=0,无 error。这只是确认 fgvad.h 本身能被 clang -fmodules 接受;modulemap 真正生效要等 SPM/Xcode 来用。

- [ ] **Step 3: 提交**

```bash
git add include/module.modulemap
git commit -m "spm: include/module.modulemap 暴露 fgvad.h 成 CFgvad C module"
```

---

## Task 2: Sources/Fgvad/FgVadAnalyzer.swift —— 单点 wrapper

**目标:** 把 iOS demo 的 `FgVadAnalyzer.swift`(已经是基本合格的克制 wrapper:Mode/ShortConfig/LongConfig/Result)抽到 `Sources/Fgvad/`,改成 public API,`import CFgvad`。两份 demo 各自的拷贝**先不删**,Task 6/7 再删,这样这一 task 不破坏 demo build。

**Files:**
- Create: `Sources/Fgvad/FgVadAnalyzer.swift`

- [ ] **Step 1: 创建 Sources/Fgvad/ 目录**

```bash
mkdir -p Sources/Fgvad
```

- [ ] **Step 2: 写 Sources/Fgvad/FgVadAnalyzer.swift**

完整代码——和 iOS demo 那份**结构相同**,但是:
- `import Foundation` 后加 `import CFgvad`(替代 demo 里通过 bridging header 看 fgvad.h)
- `final class FgVadAnalyzer` 改成 `public final class FgVadAnalyzer`
- 所有内嵌 enum / struct / func 加 `public` 修饰
- Result 里 `type/event/state/endReason` 类型从直接 `FgVadResultType` 等改成显式 `CFgvad.FgVadResultType`(好让消费方知道这是 C 类型,但因为 cbindgen 没 typedef 命名空间也无所谓——保持 `FgVadResultType` 即可,Swift 会从 CFgvad module 解析)

`Sources/Fgvad/FgVadAnalyzer.swift`:

```swift
import Foundation
import CFgvad

/// fgvad C API 的 Swift 封装。
/// - 实例版（`init(mode:)` → `start` → `feed` → `stop`）用于录音时流式喂入
/// - static `analyze(...)` 用于对已录好的整段 PCM 做批式回灌（调参重跑）
public final class FgVadAnalyzer {

    public enum Mode {
        case short(ShortConfig)
        case long(LongConfig)
    }

    /// 短时模式:命令/查询式单次交互,尾静音一到就结束整个会话。
    public struct ShortConfig {
        public var headSilenceTimeoutMs: UInt32
        public var tailSilenceMs: UInt32
        public var maxDurationMs: UInt32

        public init(
            headSilenceTimeoutMs: UInt32 = 3_000,
            tailSilenceMs: UInt32 = 2_000,
            maxDurationMs: UInt32 = 30_000
        ) {
            self.headSilenceTimeoutMs = headSilenceTimeoutMs
            self.tailSilenceMs = tailSilenceMs
            self.maxDurationMs = maxDurationMs
        }
    }

    /// 长时模式:连续听写式多句交互,外部不 stop 就一直跑。
    public struct LongConfig {
        public var headSilenceTimeoutMs: UInt32
        public var maxSentenceDurationMs: UInt32
        public var maxSessionDurationMs: UInt32
        public var tailSilenceMsInitial: UInt32
        public var tailSilenceMsMin: UInt32
        public var enableDynamicTail: Bool

        public init(
            headSilenceTimeoutMs: UInt32 = 3_000,
            maxSentenceDurationMs: UInt32 = 30_000,
            maxSessionDurationMs: UInt32 = 0,
            tailSilenceMsInitial: UInt32 = 2_000,
            tailSilenceMsMin: UInt32 = 600,
            enableDynamicTail: Bool = true
        ) {
            self.headSilenceTimeoutMs = headSilenceTimeoutMs
            self.maxSentenceDurationMs = maxSentenceDurationMs
            self.maxSessionDurationMs = maxSessionDurationMs
            self.tailSilenceMsInitial = tailSilenceMsInitial
            self.tailSilenceMsMin = tailSilenceMsMin
            self.enableDynamicTail = enableDynamicTail
        }
    }

    public struct Result {
        public var type: FgVadResultType
        public var event: FgVadEvent
        public var state: FgVadState
        public var endReason: FgVadEndReason
        public var streamOffsetSample: UInt64
        public var frameCount: Int
        public var audioLen: Int
        public var probabilities: [Float]
    }

    public enum AnalyzerError: Error {
        case createFailed
        case processFailed
    }

    // MARK: - 实例:流式

    private let vad: OpaquePointer

    public init(mode: Mode) throws {
        let handle: OpaquePointer?
        switch mode {
        case .short(let c):
            handle = fgvad_new_short(c.headSilenceTimeoutMs, c.tailSilenceMs, c.maxDurationMs)
        case .long(let c):
            handle = fgvad_new_long(
                c.headSilenceTimeoutMs,
                c.maxSentenceDurationMs,
                c.maxSessionDurationMs,
                c.tailSilenceMsInitial,
                c.tailSilenceMsMin,
                c.enableDynamicTail)
        }
        guard let handle else { throw AnalyzerError.createFailed }
        self.vad = handle
    }

    deinit { fgvad_free(vad) }

    public func start() { fgvad_start(vad) }
    public func stop() { fgvad_stop(vad) }

    /// 喂入一段 i16 PCM chunk,返回本次 chunk 产生的所有 VadResult 段。
    public func feed(_ samples: UnsafeBufferPointer<Int16>) throws -> [Result] {
        guard let handle = fgvad_process(vad, samples.baseAddress, UInt(samples.count)) else {
            throw AnalyzerError.processFailed
        }
        defer { fgvad_results_free(handle) }
        let count = Int(fgvad_results_count(handle))
        var out: [Result] = []
        out.reserveCapacity(count)
        for i in 0..<count {
            let v = fgvad_result_view(handle, UInt(i))
            let fc = Int(v.frames_count)
            let probs: [Float]
            if let ptr = v.probabilities_ptr, fc > 0 {
                probs = Array(UnsafeBufferPointer(start: ptr, count: fc))
            } else {
                probs = []
            }
            out.append(Result(
                type: v.result_type,
                event: v.event,
                state: v.state,
                endReason: v.end_reason,
                streamOffsetSample: v.stream_offset_sample,
                frameCount: fc,
                audioLen: Int(v.audio_len),
                probabilities: probs))
        }
        return out
    }

    public var state: FgVadState { fgvad_state(vad) }
    public var endReason: FgVadEndReason { fgvad_end_reason(vad) }

    // MARK: - 静态:批式回灌

    public static func analyze(
        samples: [Int16], mode: Mode
    ) throws -> (results: [Result], finalState: FgVadState, endReason: FgVadEndReason) {
        let a = try FgVadAnalyzer(mode: mode)
        a.start()
        let results = try samples.withUnsafeBufferPointer { try a.feed($0) }
        a.stop()
        return (results, a.state, a.endReason)
    }
}

// MARK: - 枚举人类可读文本(log 用)

extension FgVadResultType {
    public var label: String {
        switch self {
        case FgVadResultType_Silence: return "Silence"
        case FgVadResultType_SentenceStart: return "SentenceStart"
        case FgVadResultType_Active: return "Active"
        case FgVadResultType_SentenceEnd: return "SentenceEnd"
        default: return "Unknown(\(rawValue))"
        }
    }
}

extension FgVadEvent {
    public var label: String? {
        switch self {
        case FgVadEvent_None_: return nil
        case FgVadEvent_SentenceStarted: return "SentenceStarted"
        case FgVadEvent_SentenceEnded: return "SentenceEnded"
        case FgVadEvent_SentenceForceCut: return "SentenceForceCut"
        case FgVadEvent_HeadSilenceTimeout: return "HeadSilenceTimeout"
        case FgVadEvent_MaxDurationReached: return "MaxDurationReached"
        default: return "Event(\(rawValue))"
        }
    }
}

extension FgVadState {
    public var label: String {
        switch self {
        case FgVadState_Idle: return "Idle"
        case FgVadState_Detecting: return "Detecting"
        case FgVadState_Started: return "Started"
        case FgVadState_Voiced: return "Voiced"
        case FgVadState_Trailing: return "Trailing"
        case FgVadState_End: return "End"
        default: return "State(\(rawValue))"
        }
    }
}

extension FgVadEndReason {
    public var label: String {
        switch self {
        case FgVadEndReason_None_: return "None"
        case FgVadEndReason_SpeechCompleted: return "SpeechCompleted"
        case FgVadEndReason_HeadSilenceTimeout: return "HeadSilenceTimeout"
        case FgVadEndReason_MaxDurationReached: return "MaxDurationReached"
        case FgVadEndReason_ExternalStop: return "ExternalStop"
        default: return "EndReason(\(rawValue))"
        }
    }
}
```

- [ ] **Step 3: 提交**

```bash
git add Sources/Fgvad/FgVadAnalyzer.swift
git commit -m "spm: Sources/Fgvad/FgVadAnalyzer.swift —— 收 wrapper 单点,public API"
```

---

## Task 3: scripts/build-xcframework.sh

**目标:** 自动化打 dist/FgvadCore.xcframework + dist/TenVad.xcframework。前者含 fgvad staticlib 三 slice(iOS device + sim + macOS universal)+ headers + modulemap;后者含 ten\_vad.framework 三 slice。

**Files:**
- Create: `scripts/build-xcframework.sh`
- Modify: `.gitignore`(排除 dist/ 产物)
- Create: `dist/.gitkeep`

- [ ] **Step 1: 写脚本**

`scripts/build-xcframework.sh`:

```bash
#!/usr/bin/env bash
# 把 fgvad 三 slice 合 FgvadCore.xcframework,ten_vad 三 slice 合 TenVad.xcframework。
# 输出到 dist/。
#
# 用法:
#   scripts/build-xcframework.sh           # debug
#   scripts/build-xcframework.sh --release # release(发版用)
#
# 依赖:
#   - rustup target: aarch64-apple-ios / aarch64-apple-ios-sim /
#                    aarch64-apple-darwin / x86_64-apple-darwin
#   - macOS Xcode(xcodebuild -create-xcframework)

set -euo pipefail

PROFILE="debug"
PROFILE_FLAG=""
for arg in "$@"; do
  if [[ "$arg" == "--release" ]]; then
    PROFILE="release"
    PROFILE_FLAG="--release"
    break
  fi
done

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

DIST="$ROOT/dist"
mkdir -p "$DIST"

# 1) iOS device + iOS sim 编出来
"$ROOT/scripts/build-ios.sh" $PROFILE_FLAG

# 2) macOS universal 编出来
"$ROOT/scripts/build-macos-universal.sh" $PROFILE_FLAG

# 3) 准备 headers 目录:include/ 已经有 fgvad.h + module.modulemap,
#    xcodebuild -create-xcframework 直接拿这个目录。

# 4) 清理旧产物(xcodebuild 不接受已存在的 -output)
rm -rf "$DIST/FgvadCore.xcframework" "$DIST/TenVad.xcframework"

# 5) 合 FgvadCore.xcframework
echo "==> 合 FgvadCore.xcframework"
xcodebuild -create-xcframework \
  -library "$ROOT/target/aarch64-apple-ios/$PROFILE/libfgvad.a" \
  -headers "$ROOT/include" \
  -library "$ROOT/target/aarch64-apple-ios-sim/$PROFILE/libfgvad.a" \
  -headers "$ROOT/include" \
  -library "$ROOT/target/universal-apple-darwin/$PROFILE/libfgvad.a" \
  -headers "$ROOT/include" \
  -output "$DIST/FgvadCore.xcframework"

# 6) 合 TenVad.xcframework(从 vendor/ 三个 slice)
echo "==> 合 TenVad.xcframework"
xcodebuild -create-xcframework \
  -framework "$ROOT/vendor/ten-vad/iOS/device/ten_vad.framework" \
  -framework "$ROOT/vendor/ten-vad/iOS/simulator/ten_vad.framework" \
  -framework "$ROOT/vendor/ten-vad/macOS/ten_vad.framework" \
  -output "$DIST/TenVad.xcframework"

echo
echo "完成:"
ls -d "$DIST"/*.xcframework
```

加可执行权限:

```bash
chmod +x scripts/build-xcframework.sh
```

- [ ] **Step 2: 修改 .gitignore**

把以下追加到 `.gitignore`:

```
# Swift Package XCFramework 产物
dist/*.xcframework/
dist/*.zip
```

- [ ] **Step 3: 创建 dist/.gitkeep**

```bash
mkdir -p dist
touch dist/.gitkeep
```

- [ ] **Step 4: 跑脚本验证产物**

```bash
./scripts/build-xcframework.sh
```

预期:
- `dist/FgvadCore.xcframework/` 含 `Info.plist` + 三个 slice 子目录
- `dist/TenVad.xcframework/` 同上

```bash
ls dist/FgvadCore.xcframework/
ls dist/TenVad.xcframework/
```

预期看到三个平台的子目录(`ios-arm64`、`ios-arm64-simulator`、`macos-arm64_x86_64` 之类——具体名字 xcodebuild 自定),每个里有 .a/.framework 和 Headers/。

- [ ] **Step 5: 提交脚本 + .gitignore + .gitkeep(不提交 xcframework 产物)**

```bash
git add scripts/build-xcframework.sh .gitignore dist/.gitkeep
git commit -m "spm: build-xcframework.sh + dist/ 产物 gitignore"
```

---

## Task 4: Package.swift

**目标:** 写 SPM manifest,path-based binaryTarget(指 dist/),让消费方 `.package(path: "...")` 能接入。
GitHub Release URL 模式留给 Plan 2。

**Files:**
- Create: `Package.swift`

- [ ] **Step 1: 写 Package.swift**

```swift
// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "Fgvad",
    platforms: [
        .iOS(.v16),
        .macOS(.v13),
    ],
    products: [
        .library(name: "Fgvad", targets: ["Fgvad"]),
    ],
    targets: [
        // C ABI(staticlib)+ headers + module.modulemap,从 dist/ 引用本地 XCFramework。
        // Plan 2 切到 GitHub Release URL + checksum。
        .binaryTarget(
            name: "FgvadCore",
            path: "dist/FgvadCore.xcframework"
        ),
        .binaryTarget(
            name: "TenVad",
            path: "dist/TenVad.xcframework"
        ),
        // Swift wrapper,依赖 FgvadCore(C)+ TenVad(链接需要)。
        .target(
            name: "Fgvad",
            dependencies: ["FgvadCore", "TenVad"],
            path: "Sources/Fgvad"
        ),
    ]
)
```

- [ ] **Step 2: 跑 swift build 自检**

```bash
swift build
```

预期:**编译成功**(可能有 warning,但不应该 error)。如果报 module map 找不到、symbol 缺失等,**先停下来报告**——可能 modulemap 没被 xcframework 正确打包。

```bash
# 如果上面失败,查一下 xcframework 内的 headers 长啥样:
find dist/FgvadCore.xcframework -name "*.modulemap" -o -name "*.h"
```

预期能看到 modulemap 在每个 slice 的 Headers/ 目录里。

- [ ] **Step 3: 提交**

```bash
git add Package.swift
git commit -m "spm: Package.swift(path-based binaryTarget,本地开发模式)"
```

---

## Task 5: iOS demo 切换 SPM 消费

**目标:** iOS demo 删掉自己的 `FgVadAnalyzer.swift`,改成 `import Fgvad`(通过 SPM 路径消费)。删 fgvad.h 的 search path / library 链接 / preBuild cargo——这些都让 SPM 接管。**保留** OC 录音器的 bridging header 部分。

**Files:**
- Modify: `examples/ios/FgVadDemo/project.yml`
- Modify: `examples/ios/FgVadDemo/FgVadDemo/FgVadDemo-Bridging-Header.h`
- Delete: `examples/ios/FgVadDemo/FgVadDemo/FgVadAnalyzer.swift`
- Maybe modify: `examples/ios/FgVadDemo/FgVadDemo/*.swift`(如果用到 `FgVadAnalyzer` 的地方需要 `import Fgvad`)

- [ ] **Step 1: 在 project.yml 加 packages 依赖**

把 `targets.FgVadDemo` 块的 `dependencies:` (如果没有就新建)改成包含 Fgvad,并在文件顶层加 packages:

`examples/ios/FgVadDemo/project.yml` 顶层加:

```yaml
packages:
  Fgvad:
    path: ../../..  # 仓库根,Package.swift 在那里
```

`targets.FgVadDemo.dependencies:` 加:

```yaml
    dependencies:
      - package: Fgvad
        product: Fgvad
```

- [ ] **Step 2: 删 project.yml 里 fgvad 相关的 settings 和 preBuildScripts**

`targets.FgVadDemo.settings.base` 块删除以下字段(保留 SWIFT_VERSION / TARGETED_DEVICE_FAMILY / SWIFT_OBJC_BRIDGING_HEADER / LD_RUNPATH_SEARCH_PATHS / ENABLE_HARDENED_RUNTIME):

- 删 `HEADER_SEARCH_PATHS`(全段)
- 删 `LIBRARY_SEARCH_PATHS[sdk=iphoneos*]` 和 `[sdk=iphonesimulator*]`(全段)
- 删 `FRAMEWORK_SEARCH_PATHS[sdk=iphoneos*]` 和 `[sdk=iphonesimulator*]`(全段)
- 删 `OTHER_LDFLAGS`(全段)

`targets.FgVadDemo.preBuildScripts` 整段删除(把 cargo build 那段去掉——交给 build-xcframework.sh 离线跑)。

- [ ] **Step 3: 改 FgVadDemo-Bridging-Header.h**

`examples/ios/FgVadDemo/FgVadDemo/FgVadDemo-Bridging-Header.h` 改成只保留 OC 录音器:

```c
#ifndef FgVadDemo_Bridging_Header_h
#define FgVadDemo_Bridging_Header_h

// iOS 录音器(OC,跑在 RemoteIO AU 专用线程上)
#import "FGAudioController.h"
#import "FGIOSRecorder.h"

#endif
```

(删掉 `#import "fgvad.h"` 那行)

- [ ] **Step 4: 删除 demo 内的 FgVadAnalyzer.swift**

```bash
rm examples/ios/FgVadDemo/FgVadDemo/FgVadAnalyzer.swift
```

- [ ] **Step 5: 给所有用到 FgVadAnalyzer 的 demo Swift 文件加 import Fgvad**

```bash
grep -l "FgVadAnalyzer\|FgVadResultType\|FgVadEvent\|FgVadState\|FgVadEndReason" \
  examples/ios/FgVadDemo/FgVadDemo/*.swift
```

每个匹配文件顶部 import 区加一行:

```swift
import Fgvad
```

(放在已有的 `import Foundation` / `import UIKit` 之后)

- [ ] **Step 6: 重生成 xcodeproj 并 build**

```bash
cd examples/ios/FgVadDemo
xcodegen generate
xcodebuild -project FgVadDemo.xcodeproj -scheme FgVadDemo \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -configuration Debug build 2>&1 | tail -10
```

预期:**BUILD SUCCEEDED**。

如果报 "could not find module 'Fgvad'" 之类,**先停下来报告**——可能是 Package.swift path 算错或 dist/ 产物不存在。

- [ ] **Step 7: 提交**

```bash
git add examples/ios/FgVadDemo/project.yml \
        examples/ios/FgVadDemo/FgVadDemo/FgVadDemo-Bridging-Header.h
git rm examples/ios/FgVadDemo/FgVadDemo/FgVadAnalyzer.swift
git add examples/ios/FgVadDemo/FgVadDemo/  # 含改了 import 的 swift 文件
git commit -m "spm: iOS demo 切换通过 SPM 消费 Fgvad,删除内部 FgVadAnalyzer 拷贝"
```

---

## Task 6: macOS demo 切换 SPM 消费

**目标:** 同 Task 5 的逻辑,但目标是 macOS demo。macOS demo 没有 OC 录音器,bridging header 整个文件可以删除。

**Files:**
- Modify: `examples/macos/project.yml`
- Modify or Delete: `examples/macos/FgVadDemo-Bridging-Header.h`
- Delete: `examples/macos/FgVadDemo/FgVadAnalyzer.swift`
- Maybe modify: `examples/macos/FgVadDemo/*.swift`(加 `import Fgvad`)

- [ ] **Step 1: project.yml 加 packages 依赖**

`examples/macos/project.yml` 顶层加:

```yaml
packages:
  Fgvad:
    path: ../..  # 仓库根
```

`targets.FgVadDemo.dependencies` 块改成只保留 SnapKit + 加 Fgvad,删除 ten\_vad framework dependency:

```yaml
    dependencies:
      - package: SnapKit
      - package: Fgvad
        product: Fgvad
```

(原来的 `- framework: ../../vendor/ten-vad/macOS/ten_vad.framework` 那段整段删掉——SPM 会通过 TenVad binaryTarget 处理。)

- [ ] **Step 2: 删 settings 里 fgvad 相关字段**

`targets.FgVadDemo.settings.base` 删除:
- `HEADER_SEARCH_PATHS`
- `LIBRARY_SEARCH_PATHS`
- `OTHER_LDFLAGS`

保留 `SWIFT_OBJC_BRIDGING_HEADER`(暂时;Step 3 决定要不要去掉)、`LD_RUNPATH_SEARCH_PATHS`、`ENABLE_HARDENED_RUNTIME`。

`preBuildScripts` 整段删除。

- [ ] **Step 3: 处理 bridging header**

打开 `examples/macos/FgVadDemo-Bridging-Header.h`——它当前就只有 `#import "fgvad.h"` 一行。完全删除该文件,并把 project.yml 里的 `SWIFT_OBJC_BRIDGING_HEADER: FgVadDemo-Bridging-Header.h` 这行**也删掉**:

```bash
rm examples/macos/FgVadDemo-Bridging-Header.h
```

- [ ] **Step 4: 删除 demo 内的 FgVadAnalyzer.swift**

```bash
rm examples/macos/FgVadDemo/FgVadAnalyzer.swift
```

- [ ] **Step 5: 加 import Fgvad**

```bash
grep -l "FgVadAnalyzer\|FgVadResultType\|FgVadEvent\|FgVadState\|FgVadEndReason" \
  examples/macos/FgVadDemo/*.swift
```

每个匹配文件顶部 import 区加 `import Fgvad`。

- [ ] **Step 6: 重生成 + build**

```bash
cd examples/macos
xcodegen generate
xcodebuild -project FgVadDemo.xcodeproj -scheme FgVadDemo \
  -configuration Debug build 2>&1 | tail -10
```

预期:**BUILD SUCCEEDED**。

- [ ] **Step 7: 提交**

```bash
git add examples/macos/project.yml
git rm examples/macos/FgVadDemo-Bridging-Header.h examples/macos/FgVadDemo/FgVadAnalyzer.swift
git add examples/macos/FgVadDemo/
git commit -m "spm: macOS demo 切换通过 SPM 消费 Fgvad,删除内部拷贝和 bridging header"
```

---

## Task 7: 端到端验证(Smoke)

**目标:** 跑 iOS 和 macOS demo 的关键路径,确认 baseline 没变。

**Files:** 无新增。

- [ ] **Step 1: 验证 swift build 整体**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
swift build 2>&1 | tail -5
```

预期:无 error。

- [ ] **Step 2: macOS demo 完整跑一段录音文件**

启动 macOS demo,加载 `test-data/long/yixi-zhuzhiwei-typography.wav`,跑 long 模式 analyze,确认句数 = 85、ForceCut = 5。

```bash
cd examples/macos
xcodebuild -project FgVadDemo.xcodeproj -scheme FgVadDemo \
  -configuration Debug build -destination 'platform=macOS' 2>&1 | tail -5
# 用户手动:open build/Debug/FgVadDemo.app,点 long 模式 + 加载 yixi WAV,看句数
```

预期:UI 行为不变(句数 85 / ForceCut 5)。

如果 UI 出错(比如 `import Fgvad` 没找到、symbol 缺失),立刻报告。

- [ ] **Step 3: iOS demo build success(Simulator)**

```bash
cd examples/ios/FgVadDemo
xcodebuild -project FgVadDemo.xcodeproj -scheme FgVadDemo \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -configuration Debug build 2>&1 | tail -5
```

预期:**BUILD SUCCEEDED**。真机端到端测留给用户在 Xcode ⌘R 验证。

- [ ] **Step 4: 跑 fgvad rust 的 cargo test 没回归**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
cargo test --release 2>&1 | tail -5
```

预期:6/6 passed(本来就是,没变)。

- [ ] **Step 5: 跑 C demo 的 ctest 没回归**

```bash
cd examples/c
cmake --build build 2>&1 | tail -3
ctest --test-dir build 2>&1 | tail -3
```

预期:wav_io 1/1 passed。

- [ ] **Step 6: 不做 commit**(本 step 仅 smoke,无新文件;若 Step 2-5 中某步触发了 demo 的 build 产物变化,xcodegen 重生成的 .xcodeproj 文件可能改了——按需 add 这部分)

```bash
git status
# 如果 .xcodeproj 改了(由 xcodegen generate 触发),commit:
git add examples/ios/FgVadDemo/FgVadDemo.xcodeproj examples/macos/FgVadDemo.xcodeproj
git commit -m "spm: 重生成 xcodeproj(切换 SPM 消费后)"
```

---

## Self-Review

**1. Spec coverage:**
- ✅ Sources/Fgvad/ 单点 wrapper:Task 2
- ✅ XCFramework 打包脚本:Task 3
- ✅ Package.swift path-based binaryTarget:Task 4
- ✅ iOS demo 切换 SPM 消费:Task 5
- ✅ macOS demo 切换 SPM 消费:Task 6
- ✅ 端到端 smoke:Task 7

GitHub Release / Package.swift URL 切换 / README install 章节——**明确不在本 plan**(留给 Plan 2)。

**2. Placeholder 扫描:** 无 TBD/TODO,每段代码完整。modulemap、Package.swift、build-xcframework.sh 都给完整内容。Step 2/3 里"加 import Fgvad" 用 grep 来定位文件,因为不知道 demo 现存哪些 swift 文件用到了——这是 demo 实际状态决定的,不是 placeholder。

**3. 类型一致性:** Sources/Fgvad/FgVadAnalyzer.swift 的 public API 与 demo 里的 FgVadAnalyzer 形状完全相同(Mode/ShortConfig/LongConfig/Result),只是加 public + 加 init。demo 调用方代码无需改写,只需 `import Fgvad`。

**未列入但合理的延伸**(不阻塞本次):
- iOS demo 的 OC 录音器(FGAudioController.h / FGIOSRecorder.h):**保留在 demo 内**,因为这是 demo-specific 录音抽象,不是 fgvad library 的责任(按 [[feedback-library-vs-consumer-scope]])
- 单元测试:Sources/Fgvad/ 暂无 swift unit test。Task 7 的 smoke 已经覆盖端到端;真要加 swift test 是 Plan 2+ 的事
- Android wrapper 配置抽象(FgVad.kt 加 ShortConfig/LongConfig 与 iOS 对齐):**不在本 plan**,Android 是独立分发链路(AAR + Maven),独立成 plan

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-25-fgvad-v0.1-swift-package.md`. 两种执行选项:**

**1. Subagent-Driven(推荐)** —— 每个 task 派一个新 subagent,task 之间 review,迭代快

**2. Inline Execution** —— 当前 session 直接执行,checkpoint 间 review

**选哪个?**
