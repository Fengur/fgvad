# 纯 C CLI Demo 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `examples/c/` 下加一个纯 C99 + CMake 的 `fgvad-cli` 工具——读 WAV、跑 fgvad、打印事件流、可选 dump 每句独立 WAV。验证 cbindgen 头文件在真实 C 编译器下可用,给 Linux/embedded/C++ 集成方留参考样板。

**Architecture:** 链接 `libfgvad.dylib`(dylib 内嵌 ten\_vad framework rpath,运行时零配置);CMake 3.18+ 通过 `add_custom_command` 触发 `cargo build --release`,以 IMPORTED 库形式引用产物;自包含 WAV 解析(~100 行 C,仅支持 mono/16k/i16,不引外部依赖);POSIX `getopt_long` 解析参数;事件输出格式与三平台 demo 风格一致。

**Tech Stack:** C99 + CMake 3.18+ / 自带 getopt(macOS) / cargo build(release) / CTest(单测)。第一版仅 macOS,Linux 留作后续。

---

## File Structure

**新建:**

| 路径 | 责任 |
|---|---|
| `examples/c/CMakeLists.txt` | 顶层工程,触发 cargo build,IMPORTED 库引用 |
| `examples/c/README.md` | 构建/运行/集成说明 |
| `examples/c/src/fgvad_cli.c` | main + getopt + 主循环 |
| `examples/c/src/wav_io.h` | WAV 读写函数声明 |
| `examples/c/src/wav_io.c` | WAV 解析实现(仅 mono/16k/i16) |
| `examples/c/src/event_fmt.h` | 枚举 → 中文 label 函数声明 |
| `examples/c/src/event_fmt.c` | 实现 |
| `examples/c/tests/test_wav_io.c` | WAV 模块手写 assert 单测(CTest 注册) |

**修改:**

| 路径 | 改什么 |
|---|---|
| `README.md` | 第 225 行 `[ ] 纯 C CLI 集成示例` 打勾,补 demo 路径 |

---

## Task 1: CMake 骨架 + 触发 cargo build + 链接 libfgvad

**目标:** `cmake --build` 自动跑 `cargo build --release`,产出最小 binary `fgvad_cli`,跑起来调用 `fgvad_state(NULL)` 验证链接成功(返回 `Idle = 0`)。

**Files:**
- Create: `examples/c/CMakeLists.txt`
- Create: `examples/c/src/fgvad_cli.c`(最小骨架)

- [ ] **Step 1: 写 CMakeLists.txt**

```cmake
cmake_minimum_required(VERSION 3.18)
project(fgvad_cli C)

set(CMAKE_C_STANDARD 99)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS OFF)

# 项目根目录(examples/c 的上两级)
set(FGVAD_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../..")
set(CARGO_PROFILE "release")
set(CARGO_TARGET_DIR "${FGVAD_ROOT}/target")

if(APPLE)
  set(FGVAD_DYLIB "${CARGO_TARGET_DIR}/${CARGO_PROFILE}/libfgvad.dylib")
else()
  message(FATAL_ERROR "本 demo 第一版仅支持 macOS")
endif()

# CMake 每次 build 都尝试触发 cargo;cargo 自己负责增量。
add_custom_command(
  OUTPUT "${FGVAD_DYLIB}"
  COMMAND cargo build --release
  WORKING_DIRECTORY "${FGVAD_ROOT}"
  COMMENT "构建 libfgvad (${CARGO_PROFILE})"
)
add_custom_target(fgvad_build DEPENDS "${FGVAD_DYLIB}")

add_library(fgvad SHARED IMPORTED)
set_target_properties(fgvad PROPERTIES
  IMPORTED_LOCATION "${FGVAD_DYLIB}"
)

add_executable(fgvad_cli src/fgvad_cli.c)
target_include_directories(fgvad_cli PRIVATE "${FGVAD_ROOT}/include")
target_link_libraries(fgvad_cli PRIVATE fgvad)
target_compile_options(fgvad_cli PRIVATE -Wall -Wextra -Wpedantic)
add_dependencies(fgvad_cli fgvad_build)

# 使可执行文件能在原地找到 libfgvad.dylib(开发期方便)
if(APPLE)
  set_target_properties(fgvad_cli PROPERTIES
    BUILD_RPATH "${CARGO_TARGET_DIR}/${CARGO_PROFILE}"
  )
endif()

# CTest
enable_testing()
```

- [ ] **Step 2: 写最小 fgvad_cli.c**

```c
#include <stdio.h>
#include "fgvad.h"

int main(void) {
    enum FgVadState s = fgvad_state(NULL);
    printf("fgvad C demo: fgvad_state(NULL) = %d (expect 0=Idle)\n", (int)s);
    return s == FgVadState_Idle ? 0 : 1;
}
```

- [ ] **Step 3: 配置 + 构建**

```bash
cd examples/c
cmake -B build -G "Unix Makefiles"
cmake --build build
```

预期:CMake 配置成功,cargo build --release 跑完产出 `target/release/libfgvad.dylib`,链接 `fgvad_cli`。

- [ ] **Step 4: 跑产物**

```bash
./build/fgvad_cli
```

预期输出:
```
fgvad C demo: fgvad_state(NULL) = 0 (expect 0=Idle)
```
退出码 0。

- [ ] **Step 5: 提交**

```bash
git add examples/c/CMakeLists.txt examples/c/src/fgvad_cli.c
git commit -m "c-demo: CMake 骨架 + cargo build 触发 + libfgvad 链接验证"
```

---

## Task 2: WAV 读取模块 + 单元测试

**目标:** 实现 `wav_read_mono16k(path, &samples, &count)`,仅接受 mono/16k/i16 格式。其他格式返回错误码。先写测试再实现。

**Files:**
- Create: `examples/c/src/wav_io.h`
- Create: `examples/c/src/wav_io.c`
- Create: `examples/c/tests/test_wav_io.c`
- Modify: `examples/c/CMakeLists.txt`(注册测试)

- [ ] **Step 1: 写测试 test_wav_io.c**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include "wav_io.h"

static const char *FGVAD_ROOT_REL = "../../..";

static char *join_path(const char *suffix) {
    static char buf[1024];
    snprintf(buf, sizeof(buf), "%s/%s", FGVAD_ROOT_REL, suffix);
    return buf;
}

static void test_read_pure_silence_5s(void) {
    int16_t *samples = NULL;
    size_t count = 0;
    int rc = wav_read_mono16k(
        join_path("test-data/short/01-pure-silence-5s.wav"),
        &samples, &count);
    assert(rc == WAV_OK);
    assert(samples != NULL);
    // 5 秒 16kHz mono,允许 ±100ms 容差
    assert(count >= 16000 * 4 && count <= 16000 * 6);
    free(samples);
    printf("OK: 01-pure-silence-5s.wav read %zu samples\n", count);
}

static void test_read_normal_utterance(void) {
    int16_t *samples = NULL;
    size_t count = 0;
    int rc = wav_read_mono16k(
        join_path("test-data/short/02-normal-utterance.wav"),
        &samples, &count);
    assert(rc == WAV_OK);
    assert(count > 0);
    free(samples);
    printf("OK: 02-normal-utterance.wav read %zu samples\n", count);
}

static void test_read_nonexistent_returns_error(void) {
    int16_t *samples = NULL;
    size_t count = 0;
    int rc = wav_read_mono16k("/tmp/nonexistent.wav", &samples, &count);
    assert(rc != WAV_OK);
    assert(samples == NULL);
    printf("OK: missing file returns error\n");
}

int main(void) {
    test_read_pure_silence_5s();
    test_read_normal_utterance();
    test_read_nonexistent_returns_error();
    printf("All wav_io tests passed.\n");
    return 0;
}
```

- [ ] **Step 2: 在 CMakeLists.txt 注册测试**

把以下追加到 `enable_testing()` 之后:

```cmake
add_executable(test_wav_io tests/test_wav_io.c src/wav_io.c)
target_include_directories(test_wav_io PRIVATE src)
target_compile_options(test_wav_io PRIVATE -Wall -Wextra -Wpedantic)
add_test(NAME wav_io COMMAND test_wav_io
         WORKING_DIRECTORY ${CMAKE_BINARY_DIR})
```

- [ ] **Step 3: 写 wav_io.h(声明) + 极简 wav_io.c(故意失败,确认测试 RED)**

`examples/c/src/wav_io.h`:
```c
#ifndef FGVAD_DEMO_WAV_IO_H
#define FGVAD_DEMO_WAV_IO_H

#include <stddef.h>
#include <stdint.h>

#define WAV_OK 0
#define WAV_ERR_OPEN -1
#define WAV_ERR_FORMAT -2
#define WAV_ERR_UNSUPPORTED -3
#define WAV_ERR_IO -4

/**
 * 读取一个 mono/16kHz/i16 PCM WAV。成功返回 WAV_OK,
 * `*out_samples` 指向 malloc 出来的 i16 数组(由调用者 free),
 * `*out_count` 为采样点数。失败返回错误码,*out_samples 置空。
 */
int wav_read_mono16k(const char *path, int16_t **out_samples, size_t *out_count);

/**
 * 写一个 mono/16kHz/i16 PCM WAV。成功返回 WAV_OK。
 */
int wav_write_mono16k(const char *path, const int16_t *samples, size_t count);

#endif
```

`examples/c/src/wav_io.c`(故意只放 stub,让测试失败):
```c
#include "wav_io.h"
#include <stdlib.h>

int wav_read_mono16k(const char *path, int16_t **out_samples, size_t *out_count) {
    (void)path;
    *out_samples = NULL;
    *out_count = 0;
    return WAV_ERR_OPEN;
}

int wav_write_mono16k(const char *path, const int16_t *samples, size_t count) {
    (void)path; (void)samples; (void)count;
    return WAV_ERR_IO;
}
```

- [ ] **Step 4: 跑测试,确认失败**

```bash
cd examples/c
cmake --build build
ctest --test-dir build -V
```

预期:`test_wav_io` 在 `test_read_pure_silence_5s` 处 assertion 失败(因为返回了 `WAV_ERR_OPEN`)。

- [ ] **Step 5: 实现 wav_io.c**

替换 `examples/c/src/wav_io.c` 为完整实现:

```c
#include "wav_io.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>

#pragma pack(push, 1)
typedef struct {
    char     riff_id[4];      // "RIFF"
    uint32_t riff_size;
    char     wave_id[4];      // "WAVE"
} RiffHeader;

typedef struct {
    char     id[4];
    uint32_t size;
} ChunkHeader;

typedef struct {
    uint16_t audio_format;    // 1 = PCM
    uint16_t num_channels;
    uint32_t sample_rate;
    uint32_t byte_rate;
    uint16_t block_align;
    uint16_t bits_per_sample;
} FmtChunk;
#pragma pack(pop)

int wav_read_mono16k(const char *path, int16_t **out_samples, size_t *out_count) {
    *out_samples = NULL;
    *out_count = 0;

    FILE *fp = fopen(path, "rb");
    if (!fp) return WAV_ERR_OPEN;

    RiffHeader rh;
    if (fread(&rh, sizeof(rh), 1, fp) != 1) { fclose(fp); return WAV_ERR_IO; }
    if (memcmp(rh.riff_id, "RIFF", 4) != 0 ||
        memcmp(rh.wave_id, "WAVE", 4) != 0) {
        fclose(fp); return WAV_ERR_FORMAT;
    }

    int got_fmt = 0;
    FmtChunk fmt = {0};

    while (1) {
        ChunkHeader ch;
        if (fread(&ch, sizeof(ch), 1, fp) != 1) {
            fclose(fp);
            return got_fmt ? WAV_ERR_IO : WAV_ERR_FORMAT;
        }

        if (memcmp(ch.id, "fmt ", 4) == 0) {
            if (ch.size < sizeof(FmtChunk)) { fclose(fp); return WAV_ERR_FORMAT; }
            if (fread(&fmt, sizeof(FmtChunk), 1, fp) != 1) {
                fclose(fp); return WAV_ERR_IO;
            }
            // 跳过 fmt 扩展字段
            if (ch.size > sizeof(FmtChunk)) {
                fseek(fp, ch.size - (long)sizeof(FmtChunk), SEEK_CUR);
            }
            got_fmt = 1;
            if (fmt.audio_format != 1 ||
                fmt.num_channels != 1 ||
                fmt.sample_rate != 16000 ||
                fmt.bits_per_sample != 16) {
                fclose(fp); return WAV_ERR_UNSUPPORTED;
            }
        } else if (memcmp(ch.id, "data", 4) == 0) {
            if (!got_fmt) { fclose(fp); return WAV_ERR_FORMAT; }
            size_t n = ch.size / 2;
            int16_t *buf = (int16_t *)malloc(n * sizeof(int16_t));
            if (!buf) { fclose(fp); return WAV_ERR_IO; }
            if (fread(buf, sizeof(int16_t), n, fp) != n) {
                free(buf); fclose(fp); return WAV_ERR_IO;
            }
            *out_samples = buf;
            *out_count = n;
            fclose(fp);
            return WAV_OK;
        } else {
            // 跳过未知 chunk(LIST/INFO 等)
            fseek(fp, ch.size, SEEK_CUR);
        }
    }
}

int wav_write_mono16k(const char *path, const int16_t *samples, size_t count) {
    FILE *fp = fopen(path, "wb");
    if (!fp) return WAV_ERR_OPEN;

    uint32_t data_size = (uint32_t)(count * sizeof(int16_t));

    RiffHeader rh = { .riff_size = 36 + data_size };
    memcpy(rh.riff_id, "RIFF", 4);
    memcpy(rh.wave_id, "WAVE", 4);
    if (fwrite(&rh, sizeof(rh), 1, fp) != 1) { fclose(fp); return WAV_ERR_IO; }

    ChunkHeader fmt_h = { .size = sizeof(FmtChunk) };
    memcpy(fmt_h.id, "fmt ", 4);
    if (fwrite(&fmt_h, sizeof(fmt_h), 1, fp) != 1) { fclose(fp); return WAV_ERR_IO; }

    FmtChunk fmt = {
        .audio_format = 1, .num_channels = 1,
        .sample_rate = 16000, .byte_rate = 16000 * 2,
        .block_align = 2, .bits_per_sample = 16,
    };
    if (fwrite(&fmt, sizeof(fmt), 1, fp) != 1) { fclose(fp); return WAV_ERR_IO; }

    ChunkHeader data_h = { .size = data_size };
    memcpy(data_h.id, "data", 4);
    if (fwrite(&data_h, sizeof(data_h), 1, fp) != 1) { fclose(fp); return WAV_ERR_IO; }

    if (count > 0 && fwrite(samples, sizeof(int16_t), count, fp) != count) {
        fclose(fp); return WAV_ERR_IO;
    }
    fclose(fp);
    return WAV_OK;
}
```

- [ ] **Step 6: 跑测试,确认通过**

```bash
cd examples/c
cmake --build build
ctest --test-dir build -V
```

预期:`Passed`,所有 3 个 case 通过。

- [ ] **Step 7: 提交**

```bash
git add examples/c/src/wav_io.h examples/c/src/wav_io.c \
        examples/c/tests/test_wav_io.c examples/c/CMakeLists.txt
git commit -m "c-demo: WAV 读写模块(mono/16k/i16)+ CTest 单测"
```

---

## Task 3: 事件格式化模块

**目标:** 实现 `fgvad_event_label` / `fgvad_state_label` / `fgvad_end_reason_label` / `fgvad_result_type_label`,把 4 类枚举转中文 label,与 iOS/macOS 的 FgVadAnalyzer.swift `.label` 扩展保持文案一致。

**Files:**
- Create: `examples/c/src/event_fmt.h`
- Create: `examples/c/src/event_fmt.c`

- [ ] **Step 1: 写 event_fmt.h**

```c
#ifndef FGVAD_DEMO_EVENT_FMT_H
#define FGVAD_DEMO_EVENT_FMT_H

#include "fgvad.h"

const char *fgvad_event_label(enum FgVadEvent e);
const char *fgvad_state_label(enum FgVadState s);
const char *fgvad_end_reason_label(enum FgVadEndReason r);
const char *fgvad_result_type_label(enum FgVadResultType t);

/**
 * 把采样数(16kHz)格式化成 mm:ss.mmm 写入 buf(>=12 字节)。
 */
void fgvad_format_timestamp(uint64_t sample_offset, char *buf, size_t buf_size);

#endif
```

- [ ] **Step 2: 写 event_fmt.c(参考 iOS FgVadAnalyzer.swift 的 label 扩展)**

```c
#include "event_fmt.h"
#include <stdio.h>

const char *fgvad_event_label(enum FgVadEvent e) {
    switch (e) {
        case FgVadEvent_None_:               return "None";
        case FgVadEvent_SentenceStarted:     return "SentenceStarted";
        case FgVadEvent_SentenceEnded:       return "SentenceEnded";
        case FgVadEvent_SentenceForceCut:    return "SentenceForceCut";
        case FgVadEvent_HeadSilenceTimeout:  return "HeadSilenceTimeout";
        case FgVadEvent_MaxDurationReached:  return "MaxDurationReached";
        default:                             return "Unknown";
    }
}

const char *fgvad_state_label(enum FgVadState s) {
    switch (s) {
        case FgVadState_Idle:       return "Idle";
        case FgVadState_Detecting:  return "Detecting";
        case FgVadState_Started:    return "Started";
        case FgVadState_Voiced:     return "Voiced";
        case FgVadState_Trailing:   return "Trailing";
        case FgVadState_End:        return "End";
        default:                    return "Unknown";
    }
}

const char *fgvad_end_reason_label(enum FgVadEndReason r) {
    switch (r) {
        case FgVadEndReason_None_:              return "None";
        case FgVadEndReason_SpeechCompleted:    return "SpeechCompleted";
        case FgVadEndReason_HeadSilenceTimeout: return "HeadSilenceTimeout";
        case FgVadEndReason_MaxDurationReached: return "MaxDurationReached";
        case FgVadEndReason_ExternalStop:       return "ExternalStop";
        default:                                return "Unknown";
    }
}

const char *fgvad_result_type_label(enum FgVadResultType t) {
    switch (t) {
        case FgVadResultType_Silence:        return "Silence";
        case FgVadResultType_SentenceStart:  return "SentenceStart";
        case FgVadResultType_Active:         return "Active";
        case FgVadResultType_SentenceEnd:    return "SentenceEnd";
        default:                             return "Unknown";
    }
}

void fgvad_format_timestamp(uint64_t sample_offset, char *buf, size_t buf_size) {
    uint64_t total_ms = sample_offset / 16; // 16 samples per ms @ 16kHz
    uint64_t mm = total_ms / 60000;
    uint64_t ss = (total_ms / 1000) % 60;
    uint64_t mmm = total_ms % 1000;
    snprintf(buf, buf_size, "%02llu:%02llu.%03llu",
             (unsigned long long)mm,
             (unsigned long long)ss,
             (unsigned long long)mmm);
}
```

- [ ] **Step 3: 提交**

```bash
git add examples/c/src/event_fmt.h examples/c/src/event_fmt.c
git commit -m "c-demo: 事件/状态/结束原因 label 模块,文案对齐 iOS/macOS demo"
```

---

## Task 4: fgvad-cli 主程序(流式喂入 + 事件打印)

**目标:** 把 wav_io + event_fmt + fgvad 串起来。命令行:

```
fgvad-cli short input.wav
fgvad-cli long input.wav
```

打印事件流并在结束时给一行汇总。

**Files:**
- Modify: `examples/c/src/fgvad_cli.c`
- Modify: `examples/c/CMakeLists.txt`(把 wav_io.c + event_fmt.c 加进 fgvad_cli target)

- [ ] **Step 1: 修改 CMakeLists.txt 把 fgvad_cli 加上 wav_io.c 和 event_fmt.c**

把:
```cmake
add_executable(fgvad_cli src/fgvad_cli.c)
```
改成:
```cmake
add_executable(fgvad_cli
  src/fgvad_cli.c
  src/wav_io.c
  src/event_fmt.c
)
target_include_directories(fgvad_cli PRIVATE src)
```
(注意保留之前已有的 `target_include_directories ... ${FGVAD_ROOT}/include` 那行——CMake 允许多次调用累加。)

- [ ] **Step 2: 写完整 fgvad_cli.c**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <stdbool.h>
#include <getopt.h>
#include "fgvad.h"
#include "wav_io.h"
#include "event_fmt.h"

typedef enum { MODE_SHORT, MODE_LONG } Mode;

static void usage(const char *prog) {
    fprintf(stderr,
        "用法: %s <short|long> <input.wav> [-o <dump_dir>]\n"
        "  short      短时模式(命令/查询语义,尾静音到点结束)\n"
        "  long       长时模式(连续多句,动态尾端点)\n"
        "  -o DIR     可选,把每句切成独立 sentence_NNN.wav 写入 DIR\n",
        prog);
}

int main(int argc, char **argv) {
    if (argc < 3) { usage(argv[0]); return 2; }

    Mode mode;
    if (strcmp(argv[1], "short") == 0) mode = MODE_SHORT;
    else if (strcmp(argv[1], "long") == 0) mode = MODE_LONG;
    else { usage(argv[0]); return 2; }

    const char *input_path = argv[2];
    const char *dump_dir = NULL;

    // 从 argv[3] 起解析 -o
    int opt;
    optind = 3;
    while ((opt = getopt(argc, argv, "o:")) != -1) {
        if (opt == 'o') dump_dir = optarg;
        else { usage(argv[0]); return 2; }
    }

    // 读 WAV
    int16_t *samples = NULL;
    size_t sample_count = 0;
    int rc = wav_read_mono16k(input_path, &samples, &sample_count);
    if (rc != WAV_OK) {
        fprintf(stderr, "读取 WAV 失败: %s (rc=%d,需要 mono/16kHz/i16)\n",
                input_path, rc);
        return 1;
    }
    fprintf(stderr, "已读入 %zu 个采样点 (~%.2f 秒)\n",
            sample_count, (double)sample_count / 16000.0);

    // 创建 VAD
    struct FgVad *vad;
    if (mode == MODE_SHORT) {
        vad = fgvad_new_short(3000, 2000, 30000);
    } else {
        vad = fgvad_new_long(3000, 30000, 0, 2000, 600, true);
    }
    if (!vad) {
        fprintf(stderr, "fgvad 实例创建失败\n");
        free(samples);
        return 1;
    }

    fgvad_start(vad);

    // 一次性喂全 PCM(demo 简化;真实流式见 README)
    struct FgVadResults *results = fgvad_process(vad, samples, sample_count);
    if (!results) {
        fprintf(stderr, "fgvad_process 失败\n");
        fgvad_free(vad); free(samples);
        return 1;
    }

    size_t n = fgvad_results_count(results);
    size_t sent_idx = 0, force_cut = 0;
    char ts[16];

    for (size_t i = 0; i < n; i++) {
        struct FgVadResultView v = fgvad_result_view(results, i);
        if (v.event == FgVadEvent_None_) continue;

        fgvad_format_timestamp(v.stream_offset_sample, ts, sizeof(ts));

        if (v.event == FgVadEvent_SentenceEnded ||
            v.event == FgVadEvent_SentenceForceCut) {
            sent_idx++;
            double dur_ms = (double)v.audio_len / 16.0;
            printf("[%s] %-22s duration=%7.0fms  end_reason=%s\n",
                   ts, fgvad_event_label(v.event), dur_ms,
                   fgvad_end_reason_label(v.end_reason));
            if (v.event == FgVadEvent_SentenceForceCut) force_cut++;

            if (dump_dir) {
                char out_path[1024];
                snprintf(out_path, sizeof(out_path),
                         "%s/sentence_%03zu.wav", dump_dir, sent_idx);
                int wrc = wav_write_mono16k(out_path, v.audio_ptr, v.audio_len);
                if (wrc != WAV_OK) {
                    fprintf(stderr, "  写 %s 失败 (rc=%d)\n", out_path, wrc);
                }
            }
        } else {
            printf("[%s] %-22s state=%s\n",
                   ts, fgvad_event_label(v.event), fgvad_state_label(v.state));
        }
    }

    enum FgVadState final_state = fgvad_state(vad);
    enum FgVadEndReason final_reason = fgvad_end_reason(vad);
    fgvad_results_free(results);
    fgvad_stop(vad);
    fgvad_free(vad);
    free(samples);

    printf("---\n");
    printf("Summary: %zu 句 (ForceCut %zu),终态=%s,end_reason=%s\n",
           sent_idx, force_cut,
           fgvad_state_label(final_state),
           fgvad_end_reason_label(final_reason));
    return 0;
}
```

- [ ] **Step 3: 构建**

```bash
cd examples/c
cmake --build build
```

预期:`fgvad_cli` 链接成功,无 warning。

- [ ] **Step 4: 用 short 测试集冒烟**

```bash
./build/fgvad_cli short ../../test-data/short/02-normal-utterance.wav
```

预期:看到 SentenceStarted / SentenceEnded 各 1 条事件,Summary 显示 "1 句"。

- [ ] **Step 5: 用 long 测试集冒烟**

```bash
./build/fgvad_cli long ../../test-data/long/yixi-zhuzhiwei-typography.wav
```

预期:看到大约 80+ 条 SentenceStarted/SentenceEnded 事件(应当与已有 baseline N=85,ForceCut=5 接近——有 ±少量浮动正常)。

- [ ] **Step 6: 提交**

```bash
git add examples/c/src/fgvad_cli.c examples/c/CMakeLists.txt
git commit -m "c-demo: 主程序——argparse + 流式喂入 + 事件流打印"
```

---

## Task 5: --dump-sentences 验证

**目标:** Task 4 已实现 dump 逻辑,本 task 只是端到端验证 + 把空目录创建 fallback 加上。

**Files:**
- Modify: `examples/c/src/fgvad_cli.c`(若 dump 目录不存在则报错指导用户先 mkdir)

- [ ] **Step 1: 检查 dump_dir 存在性**

在 `if (dump_dir) { ... fgvad_start(vad); ...` 之前加:

```c
    if (dump_dir) {
        FILE *probe = NULL;
        char probe_path[1024];
        snprintf(probe_path, sizeof(probe_path), "%s/.write_probe", dump_dir);
        probe = fopen(probe_path, "wb");
        if (!probe) {
            fprintf(stderr, "dump 目录不可写: %s (请先 mkdir -p)\n", dump_dir);
            free(samples);
            return 1;
        }
        fclose(probe);
        remove(probe_path);
    }
```

- [ ] **Step 2: 端到端跑 yixi long + dump**

```bash
mkdir -p /tmp/fgvad-c-out
./build/fgvad_cli long ../../test-data/long/yixi-zhuzhiwei-typography.wav -o /tmp/fgvad-c-out
ls /tmp/fgvad-c-out/ | head -5
```

预期:`/tmp/fgvad-c-out/sentence_001.wav` ... `sentence_085.wav` 之类。

- [ ] **Step 3: 抽检一个 dump 出来的句子能播**

```bash
afplay /tmp/fgvad-c-out/sentence_001.wav
```

预期:能听到一段连贯的人声片段,不是噪音/静音。

- [ ] **Step 4: 提交**

```bash
git add examples/c/src/fgvad_cli.c
git commit -m "c-demo: dump 目录可写性预检"
```

---

## Task 6: README + roadmap 勾选

**目标:** 写 `examples/c/README.md`,给集成方说清楚怎么 build / run / 接入自己工程。在主 README 把 roadmap 那行 `[ ]` 勾上。

**Files:**
- Create: `examples/c/README.md`
- Modify: `README.md`(第 225 行)

- [ ] **Step 1: 写 examples/c/README.md**

```markdown
# fgvad C Demo

纯 C99 + CMake 的命令行工具,演示 fgvad 在不依赖任何 GUI/平台 SDK 的环境下
怎么集成。云端服务、Linux 后台任务、嵌入式集成方可以参考本目录的链接方式。

## 构建

只需要 macOS + CMake 3.18+ + Rust toolchain。

\`\`\`bash
cmake -B build
cmake --build build
\`\`\`

CMake 会自动触发顶层 `cargo build --release`,产物 `libfgvad.dylib` 落在
`<repo>/target/release/`。可执行文件落在 `examples/c/build/fgvad_cli`。

## 运行

\`\`\`bash
# 短时模式——命令/查询场景
./build/fgvad_cli short ../../test-data/short/02-normal-utterance.wav

# 长时模式——连续多句听写
./build/fgvad_cli long ../../test-data/long/yixi-zhuzhiwei-typography.wav

# 把每句切成独立 WAV
mkdir -p /tmp/fgvad-out
./build/fgvad_cli long input.wav -o /tmp/fgvad-out
\`\`\`

## 输出格式

\`\`\`
[mm:ss.mmm] EventName             state=...   或   duration=Nms  end_reason=...
\`\`\`

末尾一行 Summary 给出句数 / ForceCut 数 / 终态 / 结束原因。

## 把 fgvad 集成到自己工程

最少需要:
1. `<repo>/include/fgvad.h` 加到 include path
2. `libfgvad.dylib`(或 `libfgvad.a`)加到 link path

如果用 staticlib(`libfgvad.a`),你还需要自己处理 ten-vad 的链接:

\`\`\`
clang ... -lfgvad -framework ten_vad \\
       -F<repo>/vendor/ten-vad/macOS \\
       -Wl,-rpath,<repo>/vendor/ten-vad/macOS
\`\`\`

如果用 dylib(`libfgvad.dylib`),ten-vad framework 的 rpath 已经在 build.rs
里编进去了——直接链 fgvad 即可,运行时 dyld 会沿 rpath 找到 ten\_vad。

## 测试

\`\`\`bash
ctest --test-dir build -V
\`\`\`

跑 wav_io 模块的单元测试。

## 边界

- 第一版仅 macOS。Linux 支持需要 vendor ten-vad Linux 预编译 + build.rs 加
  `link_linux()` 分支(在主项目 roadmap 里)。
- WAV 解析仅接受 mono / 16kHz / i16,其他格式直接报错。
- 流式 vs 批式:本 demo 一次性把整段 PCM 喂给 `fgvad_process`(简单直观)。真实
  服务端流式接入时,每个网络帧到达后 chunk-by-chunk 调 `fgvad_process` 即可,
  返回的 results 处理逻辑相同。
\`\`\`

(注意:上面的代码块用了 `\`\`\``  转义,实际写入文件时是反引号。)

- [ ] **Step 2: 修改主 README.md 第 225 行**

把:
```
- [ ] 纯 C CLI 集成示例(验证 cbindgen 头文件在真实 C 编译里没 bug,给 Linux/embedded/C++ 集成方留个参考样板)
```

改成:
```
- [x] 纯 C CLI 集成示例 —— 见 [`examples/c/`](./examples/c/)(macOS 已支持;Linux/embedded 待后续)
```

- [ ] **Step 3: 提交**

```bash
git add examples/c/README.md README.md
git commit -m "c-demo: README + roadmap 勾选"
```

---

## Self-Review

**Spec coverage 检查:**
- ✅ 平台:仅 macOS(Task 1 CMakeLists 显式 FATAL_ERROR 非 APPLE)
- ✅ C99:CMakeLists 设置 `CMAKE_C_STANDARD 99`
- ✅ 默认参数 + mode + `-o`:Task 4 实现
- ✅ WAV 解析自包含:Task 2 完整实现,无外部依赖
- ✅ 命令行三种用法都覆盖到(short / long / -o)
- ✅ 单元测试:Task 2 用 CTest + 现有 test-data
- ✅ README:Task 6
- ✅ Roadmap tick:Task 6

**Placeholder 扫描:** 无 TBD/TODO/"add appropriate"等。每段代码都给出完整实现。

**类型一致:** `WAV_OK`/`WAV_ERR_*` 在 Task 2 定义并贯穿 Task 5 使用;`FgVadEvent_*` 等枚举常量与 `include/fgvad.h` 严格一致;`Mode` 枚举仅在 fgvad_cli.c 内部使用。

**未列入但合理的延伸**(不阻塞本次):
- Linux 支持:需要主项目 roadmap 的 `vendor/ten-vad/Linux` + build.rs `link_linux()`
- chunk 流式 demo:目前一次喂全 PCM。如果集成方反馈,下次加一个 `--chunk-ms 320` flag 模拟流式
- C++ wrapper(`fgvad.hpp` RAII):未来 C++ 集成方需求出现时再加

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-25-c-cli-demo.md`. 两种执行选项:**

**1. Subagent-Driven(推荐)** —— 每个 task 派一个新 subagent,task 之间 review,迭代快

**2. Inline Execution** —— 当前 session 直接执行,checkpoint 间 review

**选哪个?**
