# fgvad v0.2.0 Android AAR via JitPack 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.2.0 让 Android 集成方一行接入 `implementation 'com.github.Fengur:fgvad:v0.2.0'`(走 JitPack 自动从 GitHub tag 编 AAR);Kotlin wrapper 顺手抽 ShortConfig / LongConfig data class 与 iOS API 表面对齐。

**Architecture:** Gradle 项目放在仓库 `android/` 子目录(不污染根目录,符合 polyglot 仓库惯例);库 module `android/fgvad/` 含 Kotlin wrapper + 预编译 jniLibs(`libfgvad_android.so` + `libten_vad.so`,arm64-v8a);仓库根 `jitpack.yml` 告诉 JitPack 切到 `android/` 跑 `./gradlew :fgvad:publishToMavenLocal`;Android demo 通过 `project(":fgvad")` 内部依赖,验证库可用后打 v0.2.0 tag 触发 JitPack 远程 build。

**Tech Stack:** Android Gradle Plugin 8.5 / Kotlin 2.0 / minSdk 26 / compileSdk 36 / JDK 17 / maven-publish plugin / JitPack(仓库根 `jitpack.yml`)

---

## File Structure

**新建:**

| 路径 | 责任 |
|---|---|
| `android/settings.gradle.kts` | Gradle root 配置 + include `:fgvad` |
| `android/build.gradle.kts` | Gradle root 构建脚本 + plugins 声明 |
| `android/gradle.properties` | android.useAndroidX 等全局属性 |
| `android/gradlew`, `android/gradlew.bat`, `android/gradle/wrapper/*` | Gradle wrapper(从 demo 复制) |
| `android/fgvad/build.gradle.kts` | 库 module:Android Library + maven-publish |
| `android/fgvad/src/main/AndroidManifest.xml` | 库 minimal manifest |
| `android/fgvad/src/main/java/io/fengur/fgvad/` | 6 个 Kotlin 文件(从 demo 移过来) |
| `android/fgvad/src/main/jniLibs/arm64-v8a/libfgvad_android.so` | 预编译 native lib(入库) |
| `android/fgvad/src/main/jniLibs/arm64-v8a/libten_vad.so` | vendor ten-vad native lib(入库) |
| `android/README.md` | Android 库的开发说明 |
| `jitpack.yml` | JitPack 配置(在仓库根) |

**修改:**

| 路径 | 改什么 |
|---|---|
| `examples/android/FgVadDemo/settings.gradle.kts` | include `":fgvad"` + project path 指 `../../../android/fgvad` |
| `examples/android/FgVadDemo/app/build.gradle.kts` | 删 jniLibs sourceset + 加 `implementation(project(":fgvad"))` |
| `examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvaddemo/MainActivity.kt` | newShort/newLong 调用改用 ShortConfig/LongConfig |
| `examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/*.kt` | **删除**(已移到 android/fgvad/) |
| `examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a/*.so` | **删除**(已移到 android/fgvad/) |
| `scripts/build-android.sh` | 输出路径改为 `android/fgvad/src/main/jniLibs/arm64-v8a/`(单点) |
| `.gitignore` | jniLibs/*.so 之前是 ignore 的,现在 android/fgvad/ 下要入库,需要排除规则 |
| `README.md` | Installation 章节加 Android(JitPack)接入示例 + 路线图 Android 分发勾选 |

---

## Task 1: 创建 android/ Gradle 项目骨架

**目标:** 仓库根下 `android/` 子目录建一个 Gradle 项目,settings + build + properties + wrapper 都齐。这是后续所有 Android 库工作的容器。

**Files:**
- Create: `android/settings.gradle.kts`
- Create: `android/build.gradle.kts`
- Create: `android/gradle.properties`
- Create: `android/gradlew`, `android/gradlew.bat`, `android/gradle/wrapper/gradle-wrapper.jar`, `android/gradle/wrapper/gradle-wrapper.properties`(从 demo 复制)

- [ ] **Step 1: 创建目录**

```bash
mkdir -p /Users/fengurwang/Desktop/OldJob/fgvad/android
cd /Users/fengurwang/Desktop/OldJob/fgvad/android
```

- [ ] **Step 2: 写 `android/settings.gradle.kts`**

```kotlin
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "fgvad"
include(":fgvad")
```

注意 `rootProject.name = "fgvad"` —— 这个名字决定了 JitPack 默认 artifact ID。

- [ ] **Step 3: 写 `android/build.gradle.kts`**

```kotlin
plugins {
    id("com.android.library") version "8.5.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.0" apply false
}
```

- [ ] **Step 4: 写 `android/gradle.properties`**

```properties
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
```

- [ ] **Step 5: 复制 gradle wrapper**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad/android
cp /Users/fengurwang/Desktop/OldJob/fgvad/examples/android/FgVadDemo/gradlew .
cp /Users/fengurwang/Desktop/OldJob/fgvad/examples/android/FgVadDemo/gradlew.bat .
mkdir -p gradle/wrapper
cp /Users/fengurwang/Desktop/OldJob/fgvad/examples/android/FgVadDemo/gradle/wrapper/gradle-wrapper.jar gradle/wrapper/
cp /Users/fengurwang/Desktop/OldJob/fgvad/examples/android/FgVadDemo/gradle/wrapper/gradle-wrapper.properties gradle/wrapper/
chmod +x gradlew
```

- [ ] **Step 6: 跑一次 gradlew 看 wrapper 工作**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad/android
./gradlew --version 2>&1 | tail -10
```

预期看到 Gradle 版本信息(8.x)和 JVM 17。

如果失败,**报告**——可能 wrapper jar 不完整或 JDK 不对。

- [ ] **Step 7: 提交**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
git add android/settings.gradle.kts android/build.gradle.kts android/gradle.properties \
        android/gradlew android/gradlew.bat android/gradle/wrapper/
git commit -m "android-aar: Gradle 项目骨架(settings + build + wrapper)"
```

---

## Task 2: 创建 android/fgvad/ library module 骨架 + 移 Kotlin wrapper

**目标:** 把 demo 内部的 Kotlin wrapper(6 个 .kt 文件)抽到独立 Gradle module `android/fgvad/`,demo 内的拷贝**先不删**,Task 6 才删,这样 Task 2 不破坏 demo build。

**Files:**
- Create: `android/fgvad/build.gradle.kts`
- Create: `android/fgvad/src/main/AndroidManifest.xml`
- Create: `android/fgvad/src/main/java/io/fengur/fgvad/EndReason.kt`
- Create: `android/fgvad/src/main/java/io/fengur/fgvad/Event.kt`
- Create: `android/fgvad/src/main/java/io/fengur/fgvad/FgVad.kt`(从 demo 复制,Task 3 重构)
- Create: `android/fgvad/src/main/java/io/fengur/fgvad/Result.kt`
- Create: `android/fgvad/src/main/java/io/fengur/fgvad/ResultType.kt`
- Create: `android/fgvad/src/main/java/io/fengur/fgvad/State.kt`

- [ ] **Step 1: 创建目录**

```bash
mkdir -p /Users/fengurwang/Desktop/OldJob/fgvad/android/fgvad/src/main/java/io/fengur/fgvad
```

- [ ] **Step 2: 写 `android/fgvad/build.gradle.kts`(初版,Task 5 加 maven-publish)**

```kotlin
plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "io.fengur.fgvad"
    compileSdk = 36

    defaultConfig {
        minSdk = 26

        ndk { abiFilters += "arm64-v8a" }

        consumerProguardFiles("consumer-rules.pro")
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }

    sourceSets {
        getByName("main") {
            jniLibs.srcDirs("src/main/jniLibs")
        }
    }
}
```

- [ ] **Step 3: 写 `android/fgvad/src/main/AndroidManifest.xml`**

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
```

(库 module 的 manifest 极简,不含 application 块)

- [ ] **Step 4: 复制 6 个 Kotlin 文件**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
cp examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/EndReason.kt \
   android/fgvad/src/main/java/io/fengur/fgvad/
cp examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/Event.kt \
   android/fgvad/src/main/java/io/fengur/fgvad/
cp examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/FgVad.kt \
   android/fgvad/src/main/java/io/fengur/fgvad/
cp examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/Result.kt \
   android/fgvad/src/main/java/io/fengur/fgvad/
cp examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/ResultType.kt \
   android/fgvad/src/main/java/io/fengur/fgvad/
cp examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/State.kt \
   android/fgvad/src/main/java/io/fengur/fgvad/
```

- [ ] **Step 5: 验证 Gradle 能 sync**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad/android
./gradlew :fgvad:assembleDebug 2>&1 | tail -10
```

预期:大概率会失败,因为 jniLibs 还没拷过来 + JNI 符号缺失。**这一步只是 sync 验证,不要求完整 build 通过**——只看 Gradle 能 parse build.gradle.kts 不报错即可。

如果报 "JAVA_HOME / JDK 错"或"could not find android-library plugin",**报告**——基础 toolchain 问题。

如果报 unresolved 符号但 sync 完成,**正常**——Task 4 拷 jniLibs 后会修。

- [ ] **Step 6: 提交**

```bash
git add android/fgvad/
git commit -m "android-aar: android/fgvad library module 骨架 + Kotlin wrapper 迁移"
```

---

## Task 3: FgVad.kt 重构 —— ShortConfig / LongConfig data class

**目标:** Kotlin wrapper 引入 `ShortConfig` / `LongConfig` data class,与 iOS 的 `ShortConfig` / `LongConfig` 对齐。原 `newShort(headSilenceMs, tailSilenceMs, maxDurationMs)` / `newLong(...)` 裸 Int 位置参数 API 同时**保留**(Task 6 demo 改前不能删),作为 deprecated 但仍可用的回退。

**Files:**
- Modify: `android/fgvad/src/main/java/io/fengur/fgvad/FgVad.kt`

- [ ] **Step 1: 替换 FgVad.kt 全部内容**

```kotlin
package io.fengur.fgvad

class FgVad private constructor(private var handle: Long) : AutoCloseable {

    init {
        require(handle != 0L) { "fgvad handle is null" }
    }

    /** 短时模式:命令/查询语义,尾静音到点结束。与 iOS ShortConfig 对齐。 */
    data class ShortConfig(
        /** 开始录音后允许连续静音多久没说话就放弃。 */
        val headSilenceMs: Int = 3_000,
        /** 进入说话后,连续静音多久认为一句话说完。 */
        val tailSilenceMs: Int = 2_000,
        /** 单次会话最长时长上限(含静音),超时强制结束。 */
        val maxDurationMs: Int = 30_000,
    )

    /** 长时模式:连续多句听写,外部不 stop 不结束。与 iOS LongConfig 对齐。 */
    data class LongConfig(
        val headSilenceMs: Int = 3_000,
        val maxSentenceMs: Int = 30_000,
        /** 0 表示不限会话总时长。 */
        val maxSessionMs: Int = 0,
        val tailSilenceMsInitial: Int = 2_000,
        val tailSilenceMsMin: Int = 600,
        val enableDynamicTail: Boolean = true,
    )

    companion object {
        init {
            System.loadLibrary("fgvad_android")  // 触发依赖 libten_vad.so 也加载
        }

        /** 短时模式:推荐 API,传 [ShortConfig] data class。 */
        fun newShort(config: ShortConfig = ShortConfig()): FgVad {
            val h = nativeNewShort(config.headSilenceMs, config.tailSilenceMs, config.maxDurationMs)
            require(h != 0L) { "FgVad.newShort failed" }
            return FgVad(h)
        }

        /** 长时模式:推荐 API,传 [LongConfig] data class。 */
        fun newLong(config: LongConfig = LongConfig()): FgVad {
            val h = nativeNewLong(
                config.headSilenceMs, config.maxSentenceMs, config.maxSessionMs,
                config.tailSilenceMsInitial, config.tailSilenceMsMin, config.enableDynamicTail
            )
            require(h != 0L) { "FgVad.newLong failed" }
            return FgVad(h)
        }

        @JvmStatic external fun nativeVersion(): String

        @JvmStatic external fun nativeNewShort(
            headSilenceMs: Int, tailSilenceMs: Int, maxDurationMs: Int,
        ): Long

        @JvmStatic external fun nativeNewLong(
            headSilenceMs: Int, maxSentenceMs: Int, maxSessionMs: Int,
            tailInitMs: Int, tailMinMs: Int, enableDynamicTail: Boolean,
        ): Long

        @JvmStatic external fun nativeFree(handle: Long)
        @JvmStatic external fun nativeStart(handle: Long)
        @JvmStatic external fun nativeStop(handle: Long)
        @JvmStatic external fun nativeReset(handle: Long)
        @JvmStatic external fun nativeState(handle: Long): Int
        @JvmStatic external fun nativeEndReason(handle: Long): Int
        @JvmStatic external fun nativeProcess(handle: Long, samples: ShortArray, count: Int): Array<Result>?

        fun version(): String = nativeVersion()
    }

    fun start() = nativeStart(handle)
    fun stop() = nativeStop(handle)
    fun reset() = nativeReset(handle)
    fun state(): State = State.values()[nativeState(handle)]
    fun endReason(): EndReason = EndReason.values()[nativeEndReason(handle)]

    fun process(samples: ShortArray, count: Int = samples.size): List<Result> {
        val arr = nativeProcess(handle, samples, count)
            ?: throw IllegalStateException("fgvad process failed")
        return arr.toList()
    }

    override fun close() {
        if (handle != 0L) {
            nativeFree(handle)
            handle = 0L
        }
    }
}
```

注意:这版**完全删除**了原来的裸 Int 位置参数 `newShort(head, tail, max)` 和 `newLong(...)`——只剩 data class 形态。这是有意的 break:
- demo 调用方在 Task 6 同步改造,不会留旧调用
- v0.2.0 是新 release,API break 可接受
- 简化 API 表面,与 iOS 对齐

- [ ] **Step 2: 验证 Gradle compile 通过**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad/android
./gradlew :fgvad:compileDebugKotlin 2>&1 | tail -10
```

预期:`BUILD SUCCESSFUL`(只编 Kotlin,不需要 .so)。

如果报 unresolved reference,**报告**——可能 import 缺失或 enum 路径错。

- [ ] **Step 3: 提交**

```bash
git add android/fgvad/src/main/java/io/fengur/fgvad/FgVad.kt
git commit -m "android-aar: FgVad.kt 重构 —— ShortConfig/LongConfig data class 与 iOS 对齐"
```

---

## Task 4: 把预编译 .so 拷到 android/fgvad/jniLibs/ + 改 build-android.sh

**目标:** 用 build-android.sh 跑一次生成 release .so,然后把 `libfgvad_android.so` 和 `libten_vad.so` 入库到 `android/fgvad/src/main/jniLibs/arm64-v8a/`。**入库二进制是 trade-off,但 JitPack 远程 build 不会有 Rust toolchain,这是必要选择**。同时改 build-android.sh 让它默认输出到新路径(单点),demo 通过 Gradle project 依赖拿 .so。

**Files:**
- Create: `android/fgvad/src/main/jniLibs/arm64-v8a/libfgvad_android.so`(入库 binary)
- Create: `android/fgvad/src/main/jniLibs/arm64-v8a/libten_vad.so`(入库 binary)
- Modify: `scripts/build-android.sh`
- Modify: `.gitignore`

- [ ] **Step 1: 跑 release build 生成新 .so**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
./scripts/build-android.sh --release 2>&1 | tail -10
```

预期:`✓ Android .so 已就绪: ...examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a` + 末尾 readelf 显示 `NEEDED: libten_vad.so`。

如果 NDK 路径错,**报告**——$ANDROID_NDK_HOME 没设。

- [ ] **Step 2: 拷 .so 到新路径**

```bash
mkdir -p android/fgvad/src/main/jniLibs/arm64-v8a
cp examples/android/fgvad-jni/target/aarch64-linux-android/release/libfgvad_android.so \
   android/fgvad/src/main/jniLibs/arm64-v8a/
cp vendor/ten-vad/Android/arm64-v8a/libten_vad.so \
   android/fgvad/src/main/jniLibs/arm64-v8a/
ls -la android/fgvad/src/main/jniLibs/arm64-v8a/
```

预期:两个 .so 文件,libfgvad_android.so 大约几 MB,libten_vad.so 大约 1MB。

- [ ] **Step 3: 修改 `scripts/build-android.sh` 让默认输出到 android/fgvad/jniLibs**

修改脚本末尾的 `DEMO_LIBS` 段。打开脚本,把:

```bash
# 拷 .so 到 demo jniLibs
DEMO_LIBS="$ROOT/examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a"
mkdir -p "$DEMO_LIBS"
cp "$ROOT/examples/android/fgvad-jni/target/aarch64-linux-android/$PROFILE/libfgvad_android.so" "$DEMO_LIBS/"
cp "$ROOT/vendor/ten-vad/Android/arm64-v8a/libten_vad.so" "$DEMO_LIBS/"

echo
echo "✓ Android .so 已就绪: $DEMO_LIBS"
ls -la "$DEMO_LIBS"
echo
echo "==> 链接信息（DT_NEEDED 应该看到 libten_vad.so）："
"$TOOLCHAIN_BIN/llvm-readelf" -d "$DEMO_LIBS/libfgvad_android.so" | grep NEEDED || true
```

替换成:

```bash
# 拷 .so 到 android/fgvad library jniLibs(单点)
LIB_LIBS="$ROOT/android/fgvad/src/main/jniLibs/arm64-v8a"
mkdir -p "$LIB_LIBS"
cp "$ROOT/examples/android/fgvad-jni/target/aarch64-linux-android/$PROFILE/libfgvad_android.so" "$LIB_LIBS/"
cp "$ROOT/vendor/ten-vad/Android/arm64-v8a/libten_vad.so" "$LIB_LIBS/"

echo
echo "✓ Android .so 已就绪: $LIB_LIBS"
ls -la "$LIB_LIBS"
echo
echo "==> 链接信息(DT_NEEDED 应该看到 libten_vad.so):"
"$TOOLCHAIN_BIN/llvm-readelf" -d "$LIB_LIBS/libfgvad_android.so" | grep NEEDED || true
```

- [ ] **Step 4: 修改 `.gitignore` 排除规则**

打开 `.gitignore`,找到这行:

```
examples/android/FgVadDemo/app/src/main/jniLibs/*/*.so
```

保持(demo 不入 .so)。**追加**:

```
# android/fgvad/ 下的预编译 .so 是分发产物,**入库**(JitPack 没 Rust toolchain)
!android/fgvad/src/main/jniLibs/*/*.so
```

注:`!` 否定排除——但当前没有总排除规则匹配 `android/fgvad/...`,所以这条 `!` 实际是 noop。**改成在 .gitignore 顶部加:**

```
# android/fgvad/ 下的 .so 是分发产物,显式 git add 时不要被忽略
# (上面的 examples/android/FgVadDemo/.../jniLibs 排除规则只针对 demo,与本目录无关)
```

实际上 .gitignore 的 `examples/android/FgVadDemo/app/src/main/jniLibs/*/*.so` 已经路径限定了 demo,不会影响 `android/fgvad/`。**不需要修 .gitignore**。

跳过 Step 4。

- [ ] **Step 5: 验证 git 看到这两个 .so**

```bash
git status -- android/fgvad/src/main/jniLibs/
```

预期:列出两个 .so 文件作为 untracked。

- [ ] **Step 6: 把脚本和 .so 一起入库**

```bash
git add scripts/build-android.sh android/fgvad/src/main/jniLibs/
git commit -m "android-aar: 预编译 .so 入库 android/fgvad/jniLibs + build-android.sh 输出单点"
```

注:这次 commit 含 binary(.so 加起来几 MB),可接受。

---

## Task 5: maven-publish 配置 + publishToMavenLocal 验证

**目标:** android/fgvad/build.gradle.kts 加 maven-publish plugin,跑 `./gradlew :fgvad:publishToMavenLocal` 把 AAR 发到本地 maven。这一步验证 AAR 打包流程能跑通。

**Files:**
- Modify: `android/fgvad/build.gradle.kts`

- [ ] **Step 1: 改 build.gradle.kts 加 maven-publish**

替换 `android/fgvad/build.gradle.kts` 全部内容为:

```kotlin
plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.android")
    `maven-publish`
}

android {
    namespace = "io.fengur.fgvad"
    compileSdk = 36

    defaultConfig {
        minSdk = 26

        ndk { abiFilters += "arm64-v8a" }

        consumerProguardFiles("consumer-rules.pro")
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }

    sourceSets {
        getByName("main") {
            jniLibs.srcDirs("src/main/jniLibs")
        }
    }

    publishing {
        singleVariant("release") {
            withSourcesJar()
        }
    }
}

publishing {
    publications {
        register<MavenPublication>("release") {
            groupId = "com.github.Fengur"
            artifactId = "fgvad"
            version = "0.2.0"

            afterEvaluate {
                from(components["release"])
            }

            pom {
                name.set("fgvad")
                description.set("智能 VAD 库 —— Rust 封装 ten-vad,带状态机和动态端点策略。")
                url.set("https://github.com/Fengur/fgvad")
                licenses {
                    license {
                        name.set("MIT")
                        url.set("https://opensource.org/licenses/MIT")
                    }
                }
                developers {
                    developer {
                        id.set("Fengur")
                        name.set("Fengur")
                    }
                }
                scm {
                    connection.set("scm:git:https://github.com/Fengur/fgvad.git")
                    developerConnection.set("scm:git:https://github.com/Fengur/fgvad.git")
                    url.set("https://github.com/Fengur/fgvad")
                }
            }
        }
    }
}
```

注意 `groupId = "com.github.Fengur"` 是 JitPack 默认 group 命名,集成方写 `implementation 'com.github.Fengur:fgvad:v0.2.0'`。

- [ ] **Step 2: 创建 consumer-rules.pro(空文件,Step 1 build.gradle 引用了)**

```bash
touch /Users/fengurwang/Desktop/OldJob/fgvad/android/fgvad/consumer-rules.pro
```

- [ ] **Step 3: 跑 publishToMavenLocal**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad/android
./gradlew :fgvad:publishToMavenLocal 2>&1 | tail -15
```

预期:`BUILD SUCCESSFUL` + 看到 publishing 任务跑过。

- [ ] **Step 4: 验证 maven local 含 AAR**

```bash
ls -la ~/.m2/repository/com/github/Fengur/fgvad/0.2.0/
```

预期看到:
- `fgvad-0.2.0.aar`(几 MB,含 .so + .class)
- `fgvad-0.2.0.module`(Gradle metadata)
- `fgvad-0.2.0.pom`(Maven POM)
- `fgvad-0.2.0-sources.jar`(源码 jar)

抽检 AAR 内含 .so:

```bash
unzip -l ~/.m2/repository/com/github/Fengur/fgvad/0.2.0/fgvad-0.2.0.aar | grep -E '\.so|classes'
```

预期看到:
- `jni/arm64-v8a/libfgvad_android.so`
- `jni/arm64-v8a/libten_vad.so`
- `classes.jar`(含 Kotlin 编译产物)

如果 AAR 里没有 .so,**报告**——sourceSets 配置不对。

- [ ] **Step 5: 提交**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
git add android/fgvad/build.gradle.kts android/fgvad/consumer-rules.pro
git commit -m "android-aar: maven-publish 配置 + publishToMavenLocal 验证"
```

---

## Task 6: Demo 切换通过 :fgvad project 依赖

**目标:** Android demo 不再自带 Kotlin wrapper 和 jniLibs,改成通过 Gradle multi-project 依赖 `:fgvad`。删 demo 内拷贝 + 调用代码改用 ShortConfig/LongConfig。

**Files:**
- Modify: `examples/android/FgVadDemo/settings.gradle.kts`
- Modify: `examples/android/FgVadDemo/app/build.gradle.kts`
- Delete: `examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/*.kt` (6 个)
- Delete: `examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a/*.so`(2 个)
- Modify: `examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvaddemo/MainActivity.kt`(调用方式)

- [ ] **Step 1: settings.gradle.kts include :fgvad path**

修改 `examples/android/FgVadDemo/settings.gradle.kts`,在 `include(":app")` 之后加:

```kotlin
include(":fgvad")
project(":fgvad").projectDir = file("../../../android/fgvad")
```

完整文件应为:

```kotlin
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "FgVadDemo"
include(":app")
include(":fgvad")
project(":fgvad").projectDir = file("../../../android/fgvad")
```

- [ ] **Step 2: app/build.gradle.kts 加依赖、删 sourceset jniLibs**

打开 `examples/android/FgVadDemo/app/build.gradle.kts`。

在 `dependencies { ... }` 块加一行:

```kotlin
    implementation(project(":fgvad"))
```

在 `android { ... }` 块里**删掉** `sourceSets { ... }`(整段),因为 jniLibs 由 :fgvad 提供。

- [ ] **Step 3: 删 demo 内 Kotlin wrapper 拷贝**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
git rm examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/EndReason.kt \
       examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/Event.kt \
       examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/FgVad.kt \
       examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/Result.kt \
       examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/ResultType.kt \
       examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvad/State.kt
```

- [ ] **Step 4: 删 demo 内 jniLibs**

```bash
git rm examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a/libfgvad_android.so \
       examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a/libten_vad.so 2>&1 | head -5
```

如果这两个 .so 之前没入库(被 .gitignore 排除),不需要 git rm,直接 `rm` 即可:

```bash
rm -f examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a/libfgvad_android.so
rm -f examples/android/FgVadDemo/app/src/main/jniLibs/arm64-v8a/libten_vad.so
```

- [ ] **Step 5: 改 demo MainActivity.kt 调用**

```bash
grep -n "newShort\|newLong" examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvaddemo/*.kt
```

找到所有调用 `FgVad.newShort(...)` 和 `FgVad.newLong(...)` 的位置(应该都在 MainActivity.kt)。

把:
```kotlin
FgVad.newShort(headSilenceMs = 3000, tailSilenceMs = 2000, maxDurationMs = 30000)
```
或位置参数:
```kotlin
FgVad.newShort(3000, 2000, 30000)
```
改成:
```kotlin
FgVad.newShort(FgVad.ShortConfig(
    headSilenceMs = 3000,
    tailSilenceMs = 2000,
    maxDurationMs = 30000,
))
```

同理 `newLong`:
```kotlin
FgVad.newLong(FgVad.LongConfig(
    headSilenceMs = 3000,
    maxSentenceMs = 30000,
    maxSessionMs = 0,
    tailSilenceMsInitial = 2000,
    tailSilenceMsMin = 600,
    enableDynamicTail = true,
))
```

如果 demo 调用的是默认值,可以更简洁:`FgVad.newShort(FgVad.ShortConfig())` 或 `FgVad.newLong(FgVad.LongConfig())`。

- [ ] **Step 6: 验证 demo build**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad/examples/android/FgVadDemo
./gradlew :app:assembleDebug 2>&1 | tail -10
```

预期:`BUILD SUCCESSFUL`。

如果失败:
- "Project ':fgvad' not found": settings.gradle.kts path 算错。当前是 `../../../android/fgvad`(从 demo settings.gradle.kts 上溯三级到仓库根 + android/fgvad)。验证:`ls /Users/fengurwang/Desktop/OldJob/fgvad/android/fgvad/build.gradle.kts` 存在
- Kotlin 编译 error("unresolved reference"):MainActivity 改造没完整,**报告 grep 输出**

- [ ] **Step 7: 提交**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
git add examples/android/FgVadDemo/settings.gradle.kts \
        examples/android/FgVadDemo/app/build.gradle.kts \
        examples/android/FgVadDemo/app/src/main/java/io/fengur/fgvaddemo/
git commit -m "android-aar: demo 切换通过 :fgvad project 依赖,删除内部 wrapper 拷贝"
```

---

## Task 7: jitpack.yml + 仓库根 README 提示

**目标:** 仓库根加 `jitpack.yml` 告诉 JitPack 切到 `android/` 跑 publishToMavenLocal,JitPack 拉 git tag 时会自动按 yml 做。

**Files:**
- Create: `jitpack.yml`(仓库根)

- [ ] **Step 1: 写 `jitpack.yml`**

```yaml
# JitPack 远程构建配置 —— Android AAR 通过 android/ 子目录的 Gradle 项目产出。
# 集成方写: implementation 'com.github.Fengur:fgvad:v0.2.0'

jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.14-tem || true

install:
  - cd android && chmod +x gradlew && ./gradlew :fgvad:publishToMavenLocal -x test
```

注:
- `jdk: openjdk17` 让 JitPack 用 JDK 17(Android Gradle Plugin 8.5 要求)
- `before_install` 装 JDK 17(JitPack docker 镜像未必默认有,`|| true` 容错)
- `install` 切到 android/,跑 publishToMavenLocal,JitPack 会从 ~/.m2 抓 AAR + POM 发到 jitpack.io

- [ ] **Step 2: 提交**

```bash
git add jitpack.yml
git commit -m "android-aar: jitpack.yml —— JitPack 远程构建配置(切 android/ 跑 publishToMavenLocal)"
```

---

## Task 8: 打 v0.2.0 tag + push + JitPack 触发

**目标:** push master 含所有 Task 1-7 改动,打 v0.2.0 tag + push,JitPack 收到 tag 后自动触发远程 build。验证 JitPack build 成功。

**Files:**
- 无新文件(纯外部操作)

- [ ] **Step 1: 确认 master 是干净的**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
git status
git log --oneline -10
```

预期:工作树干净,HEAD 含 Task 1-7 所有改动(7+ commits)。

- [ ] **Step 2: push master**

```bash
git push origin master 2>&1 | tail -3
```

- [ ] **Step 3: 打 v0.2.0 tag**

```bash
git tag -a v0.2.0 -m "fgvad v0.2.0 —— Android AAR via JitPack

新增:
- Android 集成方一行接入: implementation 'com.github.Fengur:fgvad:v0.2.0'
- Kotlin wrapper 重构: ShortConfig / LongConfig data class,与 iOS API 对齐
- Android 库结构抽离至 android/fgvad/,demo 通过 multi-project 依赖
- 预编译 .so 入库 jniLibs(arm64-v8a),JitPack 远程零 Rust toolchain

仍兼容:
- v0.1.0 SPM URL / CocoaPods / 手动 XCFramework 三种 iOS/macOS 接入方式不变"

git push origin v0.2.0 2>&1 | tail -3
```

- [ ] **Step 4: 触发 JitPack 远程 build**

JitPack 收到 push 通知后会自动 build。手动触发(让 JitPack 立即 fetch tag 而不等定时任务):

```bash
curl -L "https://jitpack.io/com/github/Fengur/fgvad/v0.2.0/fgvad-v0.2.0.pom" 2>&1 | tail -20
```

第一次访问 JitPack 会触发 build,可能需要 1-3 分钟编译。返回的可能是:
- HTTP 200 + POM 内容:**build 成功**
- HTTP 404 + "Build is in progress":**还在编**,等 1-2 分钟再访问
- HTTP 404 + "Build was not successful":**build 失败**,看 JitPack 日志

- [ ] **Step 5: 看 JitPack build 日志**

```bash
curl -L "https://jitpack.io/com/github/Fengur/fgvad/v0.2.0/build.log" 2>&1 | tail -30
```

或 web:`https://jitpack.io/#Fengur/fgvad`

预期日志末尾:
```
BUILD SUCCESSFUL
... :fgvad:publishToMavenLocal completed
```

如果 BUILD FAILED:
- "JAVA_HOME" 类问题:jitpack.yml jdk 配置错——`报告`
- "Could not resolve plugin"/"missing repository":pluginManagement 在 settings.gradle.kts 里——确认 Task 1 配过
- 其他:**贴日志末尾报告**

- [ ] **Step 6: 不需要 commit**(纯外部操作)

---

## Task 9: 临时 Android consumer 验证 JitPack 接入

**目标:** 在仓库**外**建一个临时 Android 项目,模拟集成方写 `implementation 'com.github.Fengur:fgvad:v0.2.0'` 通过 JitPack 拉远程,build 验证全栈接入。

**Files:**
- 临时:`/tmp/fgvad-android-consumer/`(本 task 内创建,验证后删)

- [ ] **Step 1: 建临时 consumer 项目骨架**

```bash
rm -rf /tmp/fgvad-android-consumer
mkdir -p /tmp/fgvad-android-consumer/app/src/main/java/com/test/consumer
cd /tmp/fgvad-android-consumer
```

- [ ] **Step 2: 写 settings.gradle.kts**

`/tmp/fgvad-android-consumer/settings.gradle.kts`:

```kotlin
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url = uri("https://jitpack.io") }
    }
}

rootProject.name = "FgvadAndroidConsumer"
include(":app")
```

- [ ] **Step 3: 写 build.gradle.kts**

`/tmp/fgvad-android-consumer/build.gradle.kts`:

```kotlin
plugins {
    id("com.android.application") version "8.5.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.0" apply false
}
```

- [ ] **Step 4: 写 gradle.properties**

`/tmp/fgvad-android-consumer/gradle.properties`:

```properties
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g
```

- [ ] **Step 5: 复制 gradle wrapper**

```bash
cp /Users/fengurwang/Desktop/OldJob/fgvad/android/gradlew /tmp/fgvad-android-consumer/
cp /Users/fengurwang/Desktop/OldJob/fgvad/android/gradlew.bat /tmp/fgvad-android-consumer/
mkdir -p /tmp/fgvad-android-consumer/gradle/wrapper
cp /Users/fengurwang/Desktop/OldJob/fgvad/android/gradle/wrapper/gradle-wrapper.jar /tmp/fgvad-android-consumer/gradle/wrapper/
cp /Users/fengurwang/Desktop/OldJob/fgvad/android/gradle/wrapper/gradle-wrapper.properties /tmp/fgvad-android-consumer/gradle/wrapper/
chmod +x /tmp/fgvad-android-consumer/gradlew
```

- [ ] **Step 6: 写 app/build.gradle.kts**

`/tmp/fgvad-android-consumer/app/build.gradle.kts`:

```kotlin
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.test.consumer"
    compileSdk = 36

    defaultConfig {
        applicationId = "com.test.consumer"
        minSdk = 26
        targetSdk = 36
        versionCode = 1
        versionName = "0.1"

        ndk { abiFilters += "arm64-v8a" }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation("com.github.Fengur:fgvad:v0.2.0")
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.appcompat:appcompat:1.7.0")
}
```

- [ ] **Step 7: 写 manifest 和最小 Activity**

`/tmp/fgvad-android-consumer/app/src/main/AndroidManifest.xml`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:label="ConsumerTest">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

`/tmp/fgvad-android-consumer/app/src/main/java/com/test/consumer/MainActivity.kt`:

```kotlin
package com.test.consumer

import android.app.Activity
import android.os.Bundle
import io.fengur.fgvad.FgVad

class MainActivity : Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // 仅做编译时引用,不实际跑(consumer 验证只关心 link/编译通过)
        val vad = FgVad.newShort(FgVad.ShortConfig())
        vad.start()
        vad.stop()
        vad.close()
        println("fgvad consumer test: state = ${vad.state()}")
    }
}
```

- [ ] **Step 8: build**

```bash
cd /tmp/fgvad-android-consumer
./gradlew :app:assembleDebug 2>&1 | tail -15
```

预期看到:
- "Resolving dependencies"
- "Downloading https://jitpack.io/com/github/Fengur/fgvad/v0.2.0/..."
- `BUILD SUCCESSFUL`
- APK 在 `app/build/outputs/apk/debug/app-debug.apk`

如果 dependency resolution 失败("Could not find com.github.Fengur:fgvad:v0.2.0"):**Task 8 的 JitPack build 没成功**,先回 Task 8 修复。

如果编译 error,**报告**——可能 ShortConfig 名字不对或 FgVad import path 错。

- [ ] **Step 9: 验证 APK 内含 .so**

```bash
unzip -l /tmp/fgvad-android-consumer/app/build/outputs/apk/debug/app-debug.apk | grep -E '\.so$'
```

预期看到:
- `lib/arm64-v8a/libfgvad_android.so`
- `lib/arm64-v8a/libten_vad.so`

如果 .so 没进 APK,**报告**——AAR 打包没把 .so 嵌进去。

- [ ] **Step 10: 清理临时目录**

```bash
rm -rf /tmp/fgvad-android-consumer
```

- [ ] **Step 11: 不需要 commit**

---

## Task 10: README Installation 章节加 Android + 路线图勾选

**目标:** 主 README 加 Android 接入说明 + 把路线图里 "Android 分发"或类似条目勾上。

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 找到现有 Installation 章节**

```bash
grep -n "## Installation\|^### " /Users/fengurwang/Desktop/OldJob/fgvad/README.md | head -10
```

应该看到 `## Installation` + `### Swift Package Manager` + `### CocoaPods(仅 iOS 16+)` + `### 手动 XCFramework` + `### 接入示例`。

- [ ] **Step 2: 在 `### CocoaPods(仅 iOS 16+)` 之后,`### 手动 XCFramework` 之前插入 Android 章节**

```markdown
### Android(JitPack)

支持 **Android API 26+ / arm64-v8a**。集成方在 `settings.gradle.kts` 加 JitPack maven 仓库:

\`\`\`kotlin
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        maven { url = uri("https://jitpack.io") }
    }
}
\`\`\`

`app/build.gradle.kts` 加依赖:

\`\`\`kotlin
dependencies {
    implementation("com.github.Fengur:fgvad:v0.2.0")
}
\`\`\`

Kotlin 调用示例:

\`\`\`kotlin
import io.fengur.fgvad.FgVad

val vad = FgVad.newShort(FgVad.ShortConfig(
    headSilenceMs = 3000,
    tailSilenceMs = 2000,
    maxDurationMs = 30000,
))
vad.start()
val results = vad.process(samples)  // ShortArray (16kHz mono i16)
for (r in results) {
    if (r.event == Event.SentenceEnded) {
        // r.audioSamples 是这一句的整段 PCM
    }
}
vad.stop()
vad.close()
\`\`\`
```

- [ ] **Step 3: 找路线图里 Android 分发条目并勾选**

```bash
grep -n "Android" /Users/fengurwang/Desktop/OldJob/fgvad/README.md | head -10
```

如果路线图里有未勾选的 Android 分发相关条目,把 `[ ]` 改成 `[x]`。如果没有,在 `## 路线图`(或类似)里追加一行:

```markdown
- [x] Android 分发（JitPack） —— v0.2.0 起，详见 [Installation](#installation)
```

- [ ] **Step 4: 提交 + push**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
git add README.md
git commit -m "android-aar: README Installation 章节加 Android(JitPack)+ 路线图勾选"
git push origin master 2>&1 | tail -3
```

---

## Self-Review

**1. Spec coverage:**
- ✅ Gradle 项目结构:Task 1-2
- ✅ Kotlin wrapper 抽离 + ShortConfig/LongConfig 重构:Task 2-3
- ✅ 预编译 .so 入库 + 单点构建路径:Task 4
- ✅ AAR 打包 + maven-publish:Task 5
- ✅ Demo 切换内部依赖:Task 6
- ✅ JitPack 配置:Task 7
- ✅ v0.2.0 release + JitPack build 验证:Task 8
- ✅ Android consumer 端到端验证:Task 9
- ✅ README install + 路线图:Task 10

**2. Placeholder 扫描:** 无 TBD/TODO。每个 task 完整代码块。命令行参数都给具体值。

**3. 类型一致性:**
- `ShortConfig` / `LongConfig` 字段名:`headSilenceMs` / `tailSilenceMs` / `maxDurationMs`(short)、`headSilenceMs / maxSentenceMs / maxSessionMs / tailSilenceMsInitial / tailSilenceMsMin / enableDynamicTail`(long)——Task 3 定义,Task 6 + Task 9 + Task 10 都引用,一致
- groupId `com.github.Fengur` 在 Task 5 build.gradle.kts、Task 9 consumer dependency、Task 10 README、Task 8 JitPack URL 全部一致
- artifactId `fgvad` 在 Task 1 rootProject.name、Task 5 publishing、Task 8/9/10 引用全部一致

**4. 已知风险:**
- **Demo 通过 `project(":fgvad")` 跨目录依赖**:settings.gradle.kts path `../../../android/fgvad` 是相对路径,在 demo 目录下解析到仓库根的 android/fgvad。如果用户在 IDE 里打开 demo Gradle 项目,IDE 应当能解析(Gradle multi-project 标准模式)
- **JitPack 远程 build 第一次需要 1-3 分钟**:Task 8 Step 4 触发后等一下;如果 timeout 重试
- **预编译 .so 入库 ~5 MB**:仓库体积增长,但远小于 vendor/ten-vad framework(macOS framework 也几 MB)。可接受

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-26-fgvad-v0.2-android-aar.md`. 两种执行选项:**

**1. Subagent-Driven(推荐)** —— 每 task 派 fresh subagent,task 之间 review,迭代快

**2. Inline Execution** —— 当前 session 直接执行

**选哪个?**
