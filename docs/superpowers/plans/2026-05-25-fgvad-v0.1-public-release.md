# fgvad v0.1.0 公开发布(Plan 2)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 fgvad v0.1.0 推到公网,让集成方一行接入 —— SPM `.package(url:..., from: "0.1.0")`、CocoaPods `pod 'Fgvad', :git => '...', :tag => 'v0.1.0'` 都能跑通。**完成态**:GitHub Release v0.1.0 + Package.swift 切 url + checksum + Fgvad.podspec lint 过 + 测试 consumer 项目都能 import 成功 + 主 README 有 install 三种姿势章节。

**Architecture:** Package.swift 用环境变量 `FGVAD_LOCAL_BINARIES` 切换 path-based(dev) / url-based(consumer)模式,master HEAD 同一份文件兼容两种用法。CocoaPods 用 `vendored_frameworks` 指 GitHub Release 解压后的 XCFramework,集成方拉 git tag。

**Tech Stack:** `gh` CLI(GitHub Release)、`swift package compute-checksum`(SHA256)、`pod spec lint` / `pod install`(CocoaPods 验证)、`zip`(打包 XCFramework)。

---

## File Structure

**新建:**

| 路径 | 责任 |
|---|---|
| `Fgvad.podspec` | CocoaPods 元数据,vendored_frameworks 指 release zip 解压后 |
| `scripts/release.sh` | 自动化:build-xcframework --release + zip + checksum 输出 |
| `tmp-spm-consumer/` | 临时 SPM 消费者验证项目(本 plan 内创建,验证后删) |
| `tmp-pod-consumer/` | 临时 Podfile 消费者验证项目(同上) |

**修改:**

| 路径 | 改什么 |
|---|---|
| `Package.swift` | binary target 用 `FGVAD_LOCAL_BINARIES` 环境变量切换 path/url + checksum |
| `README.md` | 加 "Installation" 章节,SPM / CocoaPods / 手动 XCFramework 三种姿势 |
| `.gitignore` | 加 `tmp-spm-consumer/`、`tmp-pod-consumer/`、`Podfile.lock`、`Pods/` |

**Release artifact**(不入库,只上传到 GitHub Release):

- `dist/FgvadCore.xcframework.zip`
- `dist/TenVad.xcframework.zip`

---

## Task 1: scripts/release.sh + 跑 release artifact

**目标:** 写一个一键 release 脚本,跑 release build + zip + checksum,产出可上传到 GitHub Release 的两个 zip + 打印 checksum 给 Package.swift 用。

**Files:**
- Create: `scripts/release.sh`

- [ ] **Step 1: 写脚本**

`scripts/release.sh`:

```bash
#!/usr/bin/env bash
# 一键 release:跑 build-xcframework.sh --release + zip xcframework + 算 SHA256。
# 输出可直接上传到 GitHub Release 的两个 zip,以及 Package.swift binaryTarget 用的 checksum。

set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

# 1) release build
"$ROOT/scripts/build-xcframework.sh" --release

# 2) zip 两个 xcframework
cd "$ROOT/dist"
rm -f FgvadCore.xcframework.zip TenVad.xcframework.zip

echo "==> zip FgvadCore.xcframework"
zip -ry FgvadCore.xcframework.zip FgvadCore.xcframework

echo "==> zip TenVad.xcframework"
zip -ry TenVad.xcframework.zip TenVad.xcframework

# 3) 算 SHA256 checksum(swift package compute-checksum 用的格式)
echo
echo "===================================="
echo "Release artifacts ready:"
ls -lh "$ROOT/dist"/*.zip
echo
echo "Package.swift 用的 checksum:"
echo "  FgvadCore: $(swift package compute-checksum FgvadCore.xcframework.zip)"
echo "  TenVad:    $(swift package compute-checksum TenVad.xcframework.zip)"
echo "===================================="
```

加可执行权限:

```bash
chmod +x scripts/release.sh
```

- [ ] **Step 2: 跑脚本**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
./scripts/release.sh 2>&1 | tail -30
```

预期最后看到:
```
Release artifacts ready:
-rw-r--r--   ... FgvadCore.xcframework.zip
-rw-r--r--   ... TenVad.xcframework.zip

Package.swift 用的 checksum:
  FgvadCore: <64 字符 SHA256>
  TenVad:    <64 字符 SHA256>
```

把这两个 checksum **记下来**(后面 Task 3 用)。

- [ ] **Step 3: 提交脚本(zip 不入库,已经被 .gitignore 排除)**

```bash
git add scripts/release.sh
git commit -m "release: scripts/release.sh —— 一键打 release zip + 算 checksum"
```

---

## Task 2: GitHub Release v0.1.0 + 上传 zip

**目标:** 打 git tag `v0.1.0` + 创建 GitHub Release + 上传两个 zip artifact。

**Files:**
- 无新文件(纯外部操作)

- [ ] **Step 1: 确认 gh CLI 已 auth**

```bash
gh auth status 2>&1 | head -5
```

预期:看到 `Logged in to github.com as Fengur`。如果没 auth,**停下来报告**——`gh auth login` 是 user-visible 操作,需要用户在浏览器完成。

- [ ] **Step 2: 确认当前 master 是干净的可发布状态**

```bash
git status
git log --oneline -3
```

预期:工作树干净,HEAD 是合并完所有 Plan 1 + Plan 2 Task 1 的 commit。

- [ ] **Step 3: 打 tag v0.1.0**

```bash
git tag -a v0.1.0 -m "fgvad v0.1.0 —— 第一个公开发布

Swift Package + CocoaPods + 手动 XCFramework 三种接入姿势。
- iOS 16+ / macOS 13+
- 含 FgvadCore.xcframework(fgvad C ABI)+ TenVad.xcframework(ten-vad framework)
- Swift wrapper:实例 init/start/feed/stop + 静态 analyze + 4 个枚举的 .label 扩展
- 短时模式 + 长时模式(动态尾端点曲线)"
git push origin v0.1.0
```

- [ ] **Step 4: 创建 GitHub Release 并上传 zip**

```bash
gh release create v0.1.0 \
  --title "fgvad v0.1.0" \
  --notes "首个公开发布。

## 接入

### Swift Package Manager
\`\`\`swift
.package(url: \"https://github.com/Fengur/fgvad.git\", from: \"0.1.0\")
\`\`\`

### CocoaPods
\`\`\`ruby
pod 'Fgvad', :git => 'https://github.com/Fengur/fgvad.git', :tag => 'v0.1.0'
\`\`\`

### 手动
下载下面两个 \`.xcframework.zip\`,解压后拖进 Xcode 项目即可。

## 平台
- iOS 16+ device + simulator
- macOS 13+ universal(arm64 + x86_64)

## 已知瑕疵
- iOS demo 的 OC 录音器有 4 个 atomic property 警告(老代码,不影响 fgvad 库本身)" \
  dist/FgvadCore.xcframework.zip \
  dist/TenVad.xcframework.zip
```

- [ ] **Step 5: 验证 Release 已发布**

```bash
gh release view v0.1.0 --json name,tagName,assets --jq '.assets[].name'
```

预期看到:
```
FgvadCore.xcframework.zip
TenVad.xcframework.zip
```

也可以 web 验证:

```bash
gh release view v0.1.0 --web
```

(浏览器打开 GitHub Release 页面)

- [ ] **Step 6: 不需要 commit**(本 task 全是外部操作:tag + release 都已 push 到远端)

---

## Task 3: Package.swift 切换双模式(path/url)

**目标:** Package.swift 用环境变量 `FGVAD_LOCAL_BINARIES` 切换 binary target 模式 —— 设了走 dist/ 本地路径(开发期),没设走 GitHub Release URL + checksum(集成方默认)。

**Files:**
- Modify: `Package.swift`

- [ ] **Step 1: 改 Package.swift**

替换为:

```swift
// swift-tools-version: 5.9
import PackageDescription
import Foundation

// 开发期 export FGVAD_LOCAL_BINARIES=1 让 Package.swift 用 dist/ 本地 xcframework,
// 改 wrapper 后无需重新打 release zip;
// 集成方默认走 GitHub Release URL + checksum,不需要本地构建。
let useLocalBinaries = ProcessInfo.processInfo.environment["FGVAD_LOCAL_BINARIES"] != nil

let releaseTag = "v0.1.0"
let releaseBase = "https://github.com/Fengur/fgvad/releases/download/\(releaseTag)"

let fgvadCoreTarget: Target = useLocalBinaries
    ? .binaryTarget(name: "FgvadCore", path: "dist/FgvadCore.xcframework")
    : .binaryTarget(
        name: "FgvadCore",
        url: "\(releaseBase)/FgvadCore.xcframework.zip",
        checksum: "<FGVADCORE_CHECKSUM>"
      )

let tenVadTarget: Target = useLocalBinaries
    ? .binaryTarget(name: "TenVad", path: "dist/TenVad.xcframework")
    : .binaryTarget(
        name: "TenVad",
        url: "\(releaseBase)/TenVad.xcframework.zip",
        checksum: "<TENVAD_CHECKSUM>"
      )

let package = Package(
    name: "Fgvad",
    platforms: [
        .iOS(.v16),
        .macOS(.v13),
    ],
    products: [
        .library(name: "Fgvad", targets: ["Fgvad"]),
    ],
    targets: [
        fgvadCoreTarget,
        tenVadTarget,
        .target(
            name: "Fgvad",
            dependencies: ["FgvadCore", "TenVad"],
            path: "Sources/Fgvad"
        ),
    ]
)
```

把 `<FGVADCORE_CHECKSUM>` 替换成 Task 1 Step 2 输出的 FgvadCore SHA256。
把 `<TENVAD_CHECKSUM>` 替换成 TenVad SHA256。

- [ ] **Step 2: 验证 dev 模式仍能 build(local 模式)**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
FGVAD_LOCAL_BINARIES=1 swift build 2>&1 | tail -3
```

预期:`Build complete!`。

- [ ] **Step 3: 验证 url 模式拉得到 release(consumer 模式)**

```bash
# 不设环境变量,默认走 url + checksum
swift package resolve 2>&1 | tail -10
```

预期:SPM 下载 FgvadCore + TenVad zip,checksum 验证通过,resolve 成功。

如果报 "checksum mismatch" 或 "could not download",**停下来报告**——可能 Task 1 算的 checksum 有错或 GitHub Release 上传失败。

- [ ] **Step 4: 提交**

```bash
git add Package.swift
git commit -m "release: Package.swift 切双模式 —— FGVAD_LOCAL_BINARIES=1 走 path,默认走 url + checksum"
git push origin master
```

---

## Task 4: tmp SPM consumer 验证

**目标:** 在仓库**外**建一个临时 SPM 消费者项目,模拟集成方写 `.package(url: ..., from: "0.1.0")`,验证全栈接入。

**Files:**
- 临时:`/tmp/fgvad-spm-consumer/`(本 task 内创建,验证后删)

- [ ] **Step 1: 建临时 consumer**

```bash
mkdir -p /tmp/fgvad-spm-consumer
cd /tmp/fgvad-spm-consumer
swift package init --type executable --name FgvadConsumer
```

- [ ] **Step 2: 改 Package.swift 加依赖**

替换 `/tmp/fgvad-spm-consumer/Package.swift` 为:

```swift
// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "FgvadConsumer",
    platforms: [.macOS(.v13)],
    dependencies: [
        .package(url: "https://github.com/Fengur/fgvad.git", from: "0.1.0"),
    ],
    targets: [
        .executableTarget(
            name: "FgvadConsumer",
            dependencies: [
                .product(name: "Fgvad", package: "fgvad"),
            ]
        ),
    ]
)
```

- [ ] **Step 3: 改 main.swift 调 fgvad API**

替换 `/tmp/fgvad-spm-consumer/Sources/main.swift` 为:

```swift
import Fgvad

print("fgvad version smoke test")
do {
    let analyzer = try FgVadAnalyzer(mode: .short(.init()))
    print("init OK,state = \(analyzer.state.label)")
    analyzer.start()
    print("start OK,state = \(analyzer.state.label)")
    let silence = [Int16](repeating: 0, count: 16000) // 1 秒静音
    let results = try silence.withUnsafeBufferPointer { try analyzer.feed($0) }
    print("feed OK,returned \(results.count) results")
    analyzer.stop()
    print("stop OK,state = \(analyzer.state.label)")
} catch {
    print("FAILED: \(error)")
    exit(1)
}
print("==> consumer smoke OK")
```

- [ ] **Step 4: 跑 consumer**

```bash
cd /tmp/fgvad-spm-consumer
swift run 2>&1 | tail -15
```

预期看到:
```
init OK,state = Idle
start OK,state = Detecting
feed OK,returned N results
stop OK,state = End
==> consumer smoke OK
```

如果 `swift package resolve` 阶段报 checksum mismatch 或 download failed,**停下来报告**。
如果 `swift run` 阶段报符号缺失或 link 错误,**停下来报告**。

- [ ] **Step 5: 清理临时目录**

```bash
rm -rf /tmp/fgvad-spm-consumer
```

- [ ] **Step 6: 不需要 commit**(临时验证,无源码改动)

---

## Task 5: Fgvad.podspec

**目标:** 写 podspec,让集成方 `pod 'Fgvad', :git => '...', :tag => 'v0.1.0'` 能用。

**Files:**
- Create: `Fgvad.podspec`

- [ ] **Step 1: 写 Fgvad.podspec**

```ruby
Pod::Spec.new do |s|
  s.name             = 'Fgvad'
  s.version          = '0.1.0'
  s.summary          = '智能 VAD 库 —— Rust 封装 ten-vad,带状态机和动态端点策略。'
  s.description      = <<-DESC
    fgvad 是一个 Rust 写的智能语音活动检测(VAD)库,基于 ten-vad 神经网络模型,
    提供状态机封装和动态尾端点曲线策略。本 podspec 提供 iOS 16+ / macOS 13+ 的
    Swift wrapper 接入。
  DESC

  s.homepage         = 'https://github.com/Fengur/fgvad'
  s.license          = { :type => 'MIT', :file => 'LICENSE' }
  s.author           = { 'Fengur' => 'noreply@fengur.cn' }

  s.source           = {
    :git => 'https://github.com/Fengur/fgvad.git',
    :tag => "v#{s.version}"
  }

  s.ios.deployment_target = '16.0'
  s.osx.deployment_target = '13.0'
  s.swift_versions = ['5.9']

  # Swift wrapper 源码
  s.source_files = 'Sources/Fgvad/**/*.swift'

  # 远程 XCFramework(从 GitHub Release 下载)
  s.ios.vendored_frameworks = 'dist/FgvadCore.xcframework', 'dist/TenVad.xcframework'
  s.osx.vendored_frameworks = 'dist/FgvadCore.xcframework', 'dist/TenVad.xcframework'

  # CocoaPods 不直接支持远程 XCFramework URL,用 prepare_command 在 pod install
  # 时下载 + 解压两个 xcframework 到 dist/。
  s.prepare_command = <<-CMD
    set -euo pipefail
    RELEASE_BASE="https://github.com/Fengur/fgvad/releases/download/v#{s.version}"
    mkdir -p dist

    if [ ! -d dist/FgvadCore.xcframework ]; then
      echo "==> 下载 FgvadCore.xcframework.zip"
      curl -L -o /tmp/FgvadCore.xcframework.zip \
        "$RELEASE_BASE/FgvadCore.xcframework.zip"
      unzip -q -o /tmp/FgvadCore.xcframework.zip -d dist
      rm /tmp/FgvadCore.xcframework.zip
    fi

    if [ ! -d dist/TenVad.xcframework ]; then
      echo "==> 下载 TenVad.xcframework.zip"
      curl -L -o /tmp/TenVad.xcframework.zip \
        "$RELEASE_BASE/TenVad.xcframework.zip"
      unzip -q -o /tmp/TenVad.xcframework.zip -d dist
      rm /tmp/TenVad.xcframework.zip
    fi
  CMD
end
```

- [ ] **Step 2: 提交**

```bash
git add Fgvad.podspec
git commit -m "release: Fgvad.podspec —— CocoaPods 接入元数据(prepare_command 下 release zip)"
git push origin master
```

---

## Task 6: pod spec lint + 临时 Pod consumer 验证

**目标:** lint 通过 + 真的能 pod install 接入。

**Files:**
- 临时:`/tmp/fgvad-pod-consumer/`

- [ ] **Step 1: pod spec lint**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
pod spec lint Fgvad.podspec --allow-warnings 2>&1 | tail -20
```

预期最后:
```
Fgvad.podspec passed validation.
```

如果失败,看具体错误:
- "ERROR: source did not download": tag v0.1.0 没 push 或仓库私有
- "ERROR: vendored_frameworks not found": prepare_command 没成功下载——网络问题或 release zip URL 错
- "WARN ...":如果是 warning 级别的(不是 ERROR),`--allow-warnings` 已经放过了

- [ ] **Step 2: 建临时 Pod consumer**

```bash
mkdir -p /tmp/fgvad-pod-consumer
cd /tmp/fgvad-pod-consumer
mkdir -p FgvadPodTest
```

写 `/tmp/fgvad-pod-consumer/Podfile`:

```ruby
platform :ios, '16.0'
use_frameworks!

target 'FgvadPodTest' do
  pod 'Fgvad', :git => 'https://github.com/Fengur/fgvad.git', :tag => 'v0.1.0'
end
```

写一个空的 Xcode 项目骨架(用 swift package 出 dummy 不够,需要真 .xcodeproj):

```bash
cd /tmp/fgvad-pod-consumer
# 用 xcodegen 生成最简 iOS 项目
cat > project.yml <<'EOF'
name: FgvadPodTest
options:
  bundleIdPrefix: com.test
  deploymentTarget: { iOS: "16.0" }
targets:
  FgvadPodTest:
    type: application
    platform: iOS
    sources: FgvadPodTest
    info:
      path: FgvadPodTest/Info.plist
      properties:
        CFBundleName: FgvadPodTest
        UILaunchScreen: { UIColorName: "" }
EOF
mkdir -p FgvadPodTest
cat > FgvadPodTest/AppDelegate.swift <<'EOF'
import UIKit
import Fgvad

@main
class AppDelegate: UIResponder, UIApplicationDelegate {
    func application(_ application: UIApplication,
                     didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey : Any]? = nil) -> Bool {
        let a = try? FgVadAnalyzer(mode: .short(.init()))
        print("Pod consumer fgvad init: \(a != nil ? "OK" : "FAILED")")
        return true
    }
}
EOF
xcodegen generate
```

- [ ] **Step 3: pod install**

```bash
cd /tmp/fgvad-pod-consumer
pod install 2>&1 | tail -15
```

预期看到:
- "Downloading dependencies"
- "Installing Fgvad (0.1.0)"
- "Generating Pods project"
- "Integrating client project"
- "Pod installation complete!"

如果失败,**停下来报告**。

- [ ] **Step 4: build .xcworkspace 验证 Pod 接入正确**

```bash
xcodebuild -workspace FgvadPodTest.xcworkspace \
           -scheme FgvadPodTest \
           -configuration Debug \
           -destination 'generic/platform=iOS' \
           build 2>&1 | tail -10
```

预期 `BUILD SUCCEEDED`(用 `generic/platform=iOS` 避开 simulator destination 问题——只编译不跑)。

如果 Swift 编译/链接通过,Pod 集成就成功了。

- [ ] **Step 5: 清理**

```bash
rm -rf /tmp/fgvad-pod-consumer
```

- [ ] **Step 6: 不 commit**(临时验证)

---

## Task 7: README install 章节(三种接入姿势)

**目标:** 主 README 加一节,告诉集成方三种接入方式。

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 找到 README 合适位置插入**

```bash
grep -n "^## " README.md | head -10
```

找到现有标题层级,在 `## License` 之前 / 介绍段之后插入 `## Installation`。

- [ ] **Step 2: 加 Installation 章节**

在合适位置追加:

```markdown
## Installation

### Swift Package Manager(推荐)

\`\`\`swift
// Package.swift dependencies:
.package(url: "https://github.com/Fengur/fgvad.git", from: "0.1.0")
\`\`\`

target 依赖:

\`\`\`swift
.product(name: "Fgvad", package: "fgvad")
\`\`\`

Xcode 集成:File → Add Package Dependencies,粘贴 URL `https://github.com/Fengur/fgvad.git`。

### CocoaPods

\`\`\`ruby
# Podfile
pod 'Fgvad', :git => 'https://github.com/Fengur/fgvad.git', :tag => 'v0.1.0'
\`\`\`

\`pod install\` 时会自动下载 GitHub Release 上的 XCFramework zip 并解压。

### 手动

下载 [v0.1.0 Release](https://github.com/Fengur/fgvad/releases/tag/v0.1.0) 的两个 zip:
- \`FgvadCore.xcframework.zip\`
- \`TenVad.xcframework.zip\`

解压后两个 \`.xcframework\` 拖进 Xcode 项目,Embed & Sign。Swift 代码:

\`\`\`swift
import Fgvad

let analyzer = try FgVadAnalyzer(mode: .short(.init()))
analyzer.start()
let results = try samples.withUnsafeBufferPointer { try analyzer.feed($0) }
analyzer.stop()
\`\`\`

平台要求:**iOS 16+** / **macOS 13+**。
```

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "release: README 加 Installation 章节(SPM / CocoaPods / 手动 三种姿势)"
git push origin master
```

---

## Task 8: 端到端公开发布签收

**目标:** 最终确认所有公开接入路径都能用。

- [ ] **Step 1: GitHub Release 页面 web 看一眼**

```bash
gh release view v0.1.0 --web
```

确认:
- Release notes 显示三种接入姿势
- Assets 列出 FgvadCore.xcframework.zip + TenVad.xcframework.zip,可下载

- [ ] **Step 2: 确认 master HEAD 包含 Plan 2 所有 commit**

```bash
git log --oneline | head -10
```

应该看到 release.sh、Package.swift 切双模式、Fgvad.podspec、README install 这些 commit 都在 master 上。

- [ ] **Step 3: 确认 Plan 1 时验证过的本地 dev 流仍工作**

```bash
cd /Users/fengurwang/Desktop/OldJob/fgvad
FGVAD_LOCAL_BINARIES=1 swift build 2>&1 | tail -3
```

预期 `Build complete!`(local 模式)。

```bash
cd examples/macos
xcodebuild -project FgVadDemo.xcodeproj -scheme FgVadDemo -configuration Debug build 2>&1 | tail -3
```

预期 `BUILD SUCCEEDED`(macOS demo 仍能 build)。**注意:demo 通过 path: 模式 SPM 接入,不受 Package.swift 切双模式影响——因为 demo 的 project.yml 写的是 `packages.Fgvad.path: ../..`,SPM 拉到 Package.swift 后默认走 url 分支(因为环境变量没设)——所以 demo build 时会从 GitHub Release 下载 zip!**

如果 demo build 因为下载 zip 慢或失败,**停下来报告**。可能需要让 demo 设 `FGVAD_LOCAL_BINARIES=1` (project.yml.settings.base 加环境变量),或者接受 demo build 时走远程下载这个新行为。

- [ ] **Step 4: 不 commit**(纯验证)

---

## Self-Review

**1. Spec coverage:**
- ✅ 阶段 A SPM 公开:Task 1-4
- ✅ 阶段 B CocoaPods:Task 5-6
- ✅ 阶段 C README:Task 7
- ✅ 端到端签收:Task 8

**2. Placeholder 扫描:**
- `<FGVADCORE_CHECKSUM>` / `<TENVAD_CHECKSUM>` 是 Task 3 必须填的真值,**不是 placeholder**——Task 1 Step 2 输出的 checksum 用来填它们。计划明确说"把这两个值替换成 SHA256"。
- 其他无 TBD/TODO。

**3. 类型一致性:** Package.swift 双模式都返回 `Target` 类型,可被 targets 数组接受;tag `v0.1.0` 在 release create 命令、Package.swift releaseTag 变量、podspec source.tag、README install 三种姿势里都一致。

**4. 已知风险点:**
- **demo build 行为变化**(Task 8 Step 3 提到):Package.swift 切 url 后,demo SPM 接入默认从 GitHub Release 下 zip,首次 build 慢,网络断会失败。**缓解**:若用户反馈影响 dev 体验,Task 8 之后追加一个小 task 把 demo project.yml 加 `FGVAD_LOCAL_BINARIES=1` 环境变量,让 demo 强制 local 模式。
- **CocoaPods prepare_command 下 zip**:依赖网络,且 lint 时会真的下载——慢但不致命。

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-25-fgvad-v0.1-public-release.md`. 两种执行选项:**

**1. Subagent-Driven(推荐)** —— 每 task 派 fresh subagent,task 之间 review,迭代快

**2. Inline Execution** —— 当前 session 直接执行

**选哪个?**
